// Test: GoX pointer type declaration
// GoX supports pointers only for struct types
package main

type Point struct {
    x int
    y int
}

// Valid: pointer to struct type declaration
type PointRef *Point

func main() {
    // Test pointer type declaration works
    p := Point{1, 2}
    assert(p.x == 1, "p.x should be 1")
    assert(p.y == 2, "p.y should be 2")
    println("pointer_type: ALL PASSED")
}

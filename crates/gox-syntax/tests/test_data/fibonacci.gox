// Fibonacci sequence implementation
package main;

func fib(n int) int {
    if n <= 1 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

func main() {
    i := 0;
    for i < 20 {
        println(fib(i));
        i += 1;
    }
}

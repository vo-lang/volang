name: Deprecated Playground Deployment

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Playground deployment deprecated
        run: |
          echo "Playground deployment is deprecated."
          echo "Studio is deployed at the site root via .github/workflows/deploy-site.yml."

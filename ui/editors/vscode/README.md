# Volang UI Authoring

This zero-runtime extension contributes `.vo` syntax, editor configuration and
UI snippets. It contains no executable extension host code and introduces no
application npm dependency. Framework-aware diagnostics and navigation remain
owned by the Volang language toolchain; this package supplies the portable
source presentation layer used while that service connects.

# Studio 性能测量

2026-09-18，在同一台 Apple M1 Pro（10 核、macOS arm64）、Node 24.16.0、
Chromium 153.0.8010.12 上，对保留的修改前静态分发与当前静态分发分别运行测试。
两次执行均使用 Wasm VM。当前分发包含 Worker 执行、24 个文档章节、目录和代码复制。

## 结果

单位为毫秒，表内为 **p50 / p95**。

| 指标 | 修改前 | 修改后 |
| --- | ---: | ---: |
| 冷加载就绪 | 512.5 / 516.9 | 483.7 / 529.7 |
| 暖加载就绪 | 500.6 / 503.8 | 470.2 / 473.5 |
| 计数按钮 | 1.5 / 3.3 | 1.4 / 3.6 |
| 主题切换 | 3.2 / 4.7 | 3.0 / 4.5 |
| 打开弹窗 | 3.3 / 4.6 | 2.9 / 4.2 |

冷加载中位数降低约 5.6%，暖加载降低约 6.1%，弹窗降低约 12.1%。计数按钮和主题
切换的中位数略有改善；冷加载和计数按钮的 p95 增加，主题及弹窗的 p99 也有波动。
这些数据支持“小幅改善、整体接近”，不能证明全场景明显提速。

当前冷加载响应字节中位数为 1,637,902，暖加载为 5,404，包含 Worker 请求。
修改前使用页面 Resource Timing；当前改为浏览器上下文请求统计，包含响应头与
压缩响应体。两者口径不同，不比较下载量变化。Gallery 没有预先下载可选编译器或编辑器。

## 方法与限制

入口为规范 Gallery 路径，视口 1280×900，本机 HTTP 使用真实 Brotli/gzip 协商。
三轮测量各在新浏览器上下文中冷加载，再在同一上下文新开一页暖加载。每个场景先预热
三次、测量二十次，冷暖合计每场景 120 个样本。交互计时从合成原生点击到目标 DOM
状态并强制布局，不包含绘制和可信输入开销。弹窗只计打开，关闭等待完整退出动效。

加载每组只有三个样本，p95 就是最大值。修改前后分开执行，系统和 Wasm 缓存可能
已热；结果不能用于推断显著性、移动设备、弱网或线上 INP/LCP。Worker 让 guest
执行离开文档线程，但其收益仍取决于实际工作负载。这次没有引入额外运行时缓存或
复杂编译优化，也没有把功能验收等同于性能测量。

## 证据与复现

- [修改前样本、环境与产物摘要](benchmarks/studio-20260918-before.json)
- [修改后样本、环境、Worker 请求与产物摘要](benchmarks/studio-20260918-after.json)

```sh
VO_TEST_PROFILE=release node eng/ui-next/cli.mjs build --studio --static
node eng/ui-next/benchmark/studio.mjs
```

运行结果写入 `target/ui-next/benchmark/studio/report.json`，包含完整分发清单、
构建摘要、原始样本和环境。测量期间避免并行编译或浏览器测试。构建使用的 Wasm
运行时和在线编译器由 `eng/ui-next/build-runtime.mjs` 及其 `--compiler` 模式提供。
产品尺寸上限由 `eng/ui-next/studio-site-budgets.mjs` 验证；交互阈值应根据更多设备和
足够样本再制定。

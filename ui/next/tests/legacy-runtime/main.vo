// Compatibility probe for the default browser runtime during Web migration.
package main

import (
	"github.com/vo-lang/ui"
)

func App() ui.View {
	return ui.Text("Legacy runtime")
}

func main() {
	if err := ui.Mount(App); err != nil {
		panic(err.Error())
	}
}

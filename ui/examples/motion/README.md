# Motion and gestures

This example exercises the renderer-neutral `motion.Value` timeline and the
typed pointer drag recognizer. The same source runs in the Web VM, JIT, Core
Wasm AOT, native VM/JIT, and Native AOT hosts.

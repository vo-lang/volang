# Adaptive UIKit

This example switches the official navigation scaffold and responsive metric
grid across compact, medium, and expanded window size classes. Stable child
identity, semantics, and event contracts are preserved across every variant.

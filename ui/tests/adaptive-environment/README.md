# Adaptive environment probe

This E2 contract application consumes host viewport and scale inputs through
the inherited `ui.Environment`. `vo ui test --viewport` certifies compact,
medium, and expanded variants without application-owned window-size state.
Its fixed observed panel also certifies typed container measurement, size
deduplication, and the bounded layout-feedback turn in VM, JIT, Web, and
desktop hosts.

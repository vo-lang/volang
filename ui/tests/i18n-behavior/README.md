# Internationalization behavior probe

This E3 probe certifies locale canonicalization, RTL direction, representative
CLDR-style cardinal plurals, grouped integer and decimal formatting, date
ordering, grapheme segmentation, deterministic collation, mixed-direction
isolation, and generated-message interpolation on every execution backend.

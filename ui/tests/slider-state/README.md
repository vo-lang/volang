# Slider state regression

Exercises a controlled floating-point slider through the renderer-neutral
input event contract. The visible `current=` and `event=` values must advance
together in VM, JIT, Web, and native hosts.

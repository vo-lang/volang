# Modal validation form probe

This E2 contract application uses the inherited UIKit theme, `func()` and
`func(string)` callbacks, typed shortcuts, modal focus isolation, autofocus,
validation semantics, Escape/backdrop dismissal, and focus restoration. The
application source never consumes the renderer's generic event envelope.

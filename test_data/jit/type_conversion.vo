// Type conversion tests - basic integer operations
package main

func main() {
    // Basic integer operations (no float conversion for now)
    a := 42
    b := a * 2
    assert(b == 84)
    
    // Negative numbers
    neg := -10
    pos := -neg
    assert(pos == 10)
}

// BUG: Array of struct causes segfault
// Minimal reproduction

package main


import "fmt"
type Inner struct {
    x int
    s string
    b bool
    f float64
}

func main() {
    var arr [3]Inner  // <- crashes here
    assert(arr[0].x == 0, "should be zero")
    arr[1].x = 100
    assert(arr[1].x == 100, "should be 100")
    fmt.Println("ok")
}

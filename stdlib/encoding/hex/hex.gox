package hex

// Extern functions (implemented in Rust)
extern func EncodeToString(src []byte) string
extern func DecodeString(s string) ([]byte, error)

// GoX-implemented functions

// EncodedLen returns the length of an encoding of n source bytes.
func EncodedLen(n int) int {
    return n * 2
}

// DecodedLen returns the length of a decoding of n source bytes.
func DecodedLen(n int) int {
    return n / 2
}

# Core 动态零值写屏障实验 · 2026-09-12

该候选不保留，已将生成器精确恢复到 Caller 修复后的对照版本。61 项目录、两版本、各三次，共 366 次工作量诊断全部正确；每项的宿主操作、帧、内存、GC、调度计数在六次执行中完全一致。全部 61 个 Wasm 镜像增大 448–6,615 字节，中位增加 553 字节，增幅 0.066%–1.853%。

候选在增量屏障开启时，为动态完整 `i64.store` 增加零值判断；动态零值跳过没有标记工作的宿主调用，非零值和部分槽写入保持原路径。原有编译期已知零值消除继续保留。新增判断没有覆盖出目录中的有效冗余流量，因此停止扩大公共存储路径；后续若取得实际动态空引用写入热点，再独立评估。

例如 binary-trees 的内存写宿主调用仍为 11,841,904 次，对象分配 6,444,383 次、帧创建和释放各 6,488,065 次。其主要跨界成本需要从帧和分配架构继续处理。

候选通过 33 项 Rust owning 检查，另一个显式执行的 Node/Wasm 测试覆盖 80 次真实存储及预期宿主调用。完整产品检查为 Core 1,145 项、宿主 68 项、真实 Chromium 26 项，共 1,239 项；目录准备另核对 183 次输出。正确性通过仅证明候选行为成立，未证明性能收益。未启动该候选的正式计时，也不报告速度变化。

证据根目录为 `target/bench/runs/toolchain-optimization-20260909/core-null-store-barrier/v1/`：

- 冻结候选：`products/1789153478824266000`，对照为 `caller-exact-debug/v1/full-products/1789152826429927000`。
- 完整盘点：`catalog-inventory/1789153853598962000`；工作量：`counters/primary`。
- `decision.json` 绑定正确性、源码、盘点及计数；候选产品的 `rejected.json` 阻止误作后续基线。
- 原始失败、测试源码、冻结产品及尚未计时的准备目录全部保留。恢复后的生成器 SHA-256 为 `97fa5ba369da10d54dde8a503ba8b3b653085434799d12009e4b3baa38c34693`。

该结论关闭动态零值屏障候选。X04 的持久帧范围和 X05 的生成代码分配路径继续开放。

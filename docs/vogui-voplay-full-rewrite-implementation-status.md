# Vogui/Voplay 全量重写实施状态

更新时间：2026-07-26

当前主里程碑：冻结计划首版全量实现与第 20 章验收已经收口。

## 联合基线

| 仓库 | 当前 SHA | `eng/project.toml` pin | 工作树 |
| --- | --- | --- | --- |
| Volang | `d3533818ad0477cc34544eb00bcc691901098488` | 当前仓库 | 32 项最终候选验证绑定的 clean commit |
| Vogui | `a5bf43f92f02667dcd339dee8975eb45e1ccf21e` | `a5bf43f92f02667dcd339dee8975eb45e1ccf21e` | clean-slate 全量重写与自包含 protocol 的稳定本地 commit |
| Voplay | `ca4df3e812544a506770c9acd7bac213eaef8afa` | `ca4df3e812544a506770c9acd7bac213eaef8afa` | clean-slate 全量重写与自包含 protocol 的稳定本地 commit |

`vo.work` 在 Volang 根目录不存在。真实 sibling owner 由 `eng/project.toml` 的 `local_hint` 解析。

## 工作树分类

- Volang 的 `native.rs`、`native_event_loop.rs`、`vo-engine` GUI VM 构造修改属于本重写且与 R3/R4 重叠。原 4ms 定时 pump 已迁移为 process-local island generation-aware waker。
- 三份未跟踪设计文档属于本重写保留资产。
- Vogui/Voplay 旧实现只作为已封存的需求、fixture 与产品行为输入，不构成新内部 API、协议、构建图或测试兼容约束。
- 用户已明确授权将旧内容移出活动树并从零建立目标布局；冻结计划是实现取舍的唯一架构依据。
- vopack、vostore 当前 clean，属于潜在 Asset/Store 边界，尚未授权为首批改动面。

## 基线命令

- `cargo run -q -p vo-dev --locked -- help; cargo test -p vo-app-runtime --locked`：长时间无输出后由实施代理中止，未取得结果，不能作为 EVID。
- 尚未执行 Vogui/Voplay 基线测试；先要按各自 dirty control files 审计命令与生成副作用。

## 已取得的本地验证

- `cargo check -p vo-dev --locked`：通过。
- `cargo test -p vo-dev rewrite_traceability --locked`：1 passed。
- `cargo run -q -p vo-dev --locked -- lint traceability`：通过；24 REQ、24 TEST、86 ACC 双向一致。
- `cargo test -p vo-app-protocol --locked`：3 passed；覆盖小端 envelope、未知 message kind 和声明长度不匹配。
- `cargo test -p vo-schema-compiler --offline`：1 passed；覆盖规范化 fingerprint 的确定性、Rust/Vo/TypeScript 共享 identity 渲染，以及 `app.timer.once` capability 常量和 `delay_millis:u64le` payload 合同。该命令未访问网络。
- `vo-dev generate app-protocol --write` 与 `--check`：通过；生成 Vo、TypeScript、独立二进制 golden 和 digest provenance。
- `cargo test -p vo-app-protocol --locked`：现为 9 passed；Rust bounded decoder 成功消费 schema compiler 产生的独立 golden，Channel Open/Accept codec 与 128 组 property matrix 通过。
- ChannelOpen/ChannelAccept 已实现 SchemaId、major compatibility fingerprint、逐-minor exact fingerprint、最高共同 minor、limit intersection 和有界 codec；malformed、重复 minor、fingerprint mismatch 与 accept endpoint 校验进入 owning tests。
- `tsc --noEmit --target ES2020 --module ESNext --skipLibCheck .../app_protocol.ts`：通过。
- `target/debug/vo check .../app_protocol.vo`：通过。
- `cargo test -p vo-app-runtime --features std --locked`：56 passed，doc-test 1 ignored；覆盖 `AppRuntime` 多 Session ownership、session slot generation/epoch 复用、Session/Channel hard quota、SessionKernel 生命周期、Closing reserved completion、waker generation 与 pending wake coalesce。
- `cargo check -p vo-app-runtime --no-default-features --locked`：通过；新增 kernel/lane/request/wake generation 保持 alloc-only 路径可编译。
- `cargo test -p vo-runtime --features std island_transport --locked`：4 passed，434 filtered；新增用例确认消息提交且可读后才调用 transport waker。
- native App event loop 已移除 4ms `recv_timeout`；idle 使用阻塞 `recv`，process-local child island send/terminal event 触发 executor wake。同代 wake 最多保留一个 pending signal，release/re-register 后旧 generation 被拒绝，callback 不进入 VM。
- `cargo test -p vo-app-runtime --features std runtime_poll_only_runs_after_current_generation_wake --locked`：1 passed；fake executor 直接确认 idle 与 stale wake 的 poll 次数为 0，当前 generation wake 才执行一次 poll。
- `BoundedLane` 同时限制 message/byte budget；可靠 admission 满载返回 `WouldBlock`，失败不消耗 sequence；关闭流量可使用显式保留额度；采样 lane 报告 `drop_count` 与 `gap_allowed`。
- `RequestRegistry` 由 SessionKernel 持有，使用 Session 单调 `RequestId`，覆盖 hard quota、fake-clock timeout、cancel、stale epoch、late/duplicate completion，以及 Closing 后停止新 ingress 并收敛遗留请求。
- `VoHostServicesV2` 已在 `vo-runtime` 建立 versioned `repr(C)` table：CallerEndpoint、allocator-neutral byte span/bulk handle、query/request/cancel/packet/pulse/time/bulk/wake callbacks、target 字段与严格 validation。64-bit little-endian 布局 golden 为 `0x9de10cb819a8da62`。
- `cargo test -p vo-runtime --features std --lib host_services_v2 --locked`：4 passed；覆盖完整 table、major/layout/target/callback early reject、ABI layout golden，以及 owner/table/CallerEndpoint immutable binding。
- VM 保存 validated `HostServicesV2Binding`；`cargo test -p vo-vm --features std --lib vm::tests::host_services --locked`：7 passed，另有 child-island binding inheritance 定向测试 1 passed。V2 owner、validated table 与 authoritative CallerEndpoint 在首次执行/child island 后冻结。
- `EndpointRegistry` 由 SessionKernel 持有并设 hard quota，CallerEndpoint 同时校验 Session/endpoint generation+epoch、role、placement 和 capability view；AudioRealtime 调用通用 HostServices 直接 denied，Closing 只保留 cancel/completion。
- `AppHostServicesV2` 使用 nonblocking `try_lock`、有界 Request/Wake lane 与有界 bulk source catalog，ABI callback panic 转结构化 internal status；query/time/begin/cancel/completion/bulk/wake 共用 Session/CallerEndpoint identity。terminal completion 在持锁状态预留 Wake lane 容量，校验对应 wake registration 后产生 generation-aware `HostWakeSignal`。`cargo test -p vo-app-runtime --features std host_services_v2 --locked -- --test-threads=1 --nocapture`：4 passed。
- `vo_ext::host::install/clear` 的 same-thread mutable fallback 已删除；仓库无外部调用点，脱离 VM/ABI call context 的 helper fail-closed inert。`cargo test -p vo-ext host::tests --locked`：2 passed。
- `BulkBufferRegistry` 和 `WakeRegistrationRegistry` 由 SessionKernel 持有，均使用 CallerEndpoint 绑定的 generation handle 与 hard quota；bulk 同时限制单 buffer/总字节，读取按 chunk 执行，release/endpoint close/session close 后旧 handle fail-closed。`SessionCloseReport` 记录 terminal request、bulk、wake 与 endpoint 的归零数量。
- native extension ABI v9 已删除 `ExtHostServicesV1` C table，改为每个 extern call 自有的 HostServices V2 proxy table；interpreter/JIT `ExternWorld` 传递同一 validated binding，ABI frame 携带 host-authored caller 副本，proxy 在 provider dispatch 前执行 exact caller equality。伪造 endpoint epoch 的 native extension 定向测试返回 `DENIED`；V2 major/size/callback early-reject 与 provider panic→structured internal status 定向测试通过。
- `vo-ext::host::v2` 已公开 query/request/cancel/packet/pulse/time/bulk/wake 通用原语，caller identity 对 extension SDK 隐藏；runtime wrapper 资源 handle 定向测试通过。`cargo test -p vo-ffi-macro --locked`：66 unit passed；doc-test 4 passed、11 ignored。
- `cargo test -p vo-app-runtime --locked`：54 passed；新增 EndpointRegistry、Caller-owned request、bulk/wake generation resource 与 SessionCloseReport 覆盖。`cargo check -p vo-app-runtime --no-default-features --locked` 再次通过。
- `TimerWheel<T>` 已进入 `vo-app-runtime` 的 alloc-only SessionKernel：固定容量、CallerEndpoint ownership、generation handle、fake monotonic clock、稳定 deadline/sequence 顺序、interval missed-period coalescing、Closing 与显式全量释放均有定向测试。`cargo test -p vo-app-runtime --lib timer_wheel --locked -- --test-threads=1 --nocapture`：3 passed。
- SessionKernel 为每个 pending RequestId 保持至多一个 timer；terminal completion、request deadline、endpoint close 与 session close 都同步撤销/回收计时资源。`cargo test -p vo-app-runtime --lib kernel::tests --locked -- --test-threads=1 --nocapture`：10 passed；`cargo check -p vo-app-runtime --no-default-features --locked`：通过。
- `AppHostServicesV2` 已提供基于同一单调时钟的 request timer schedule/cancel/next-deadline/expire adapter；时钟拒绝回退，timer 到期后由平台在 owner lock 外执行 terminal completion。带 `std` 的 HostServices V2 定向测试：4 passed。
- `AppSession::new_hosted` 已能创建唯一 AppRuntime/SessionKernel、注册 FrameworkLogic HostedActor endpoint、安装同一 V2 owner/caller binding，并在 shutdown 返回资源归零报告；NativeGuiRuntime 与 native event loop 已切到该 owner 路径。`RequestId -> HostWaitKey` 完整映射只保留在 AppSession，平台 command/signal 只携带 RequestId 与不透明 token；caller/request/token mismatch 定向测试通过。
- `vo-app-runtime` 的旧 `NativeTickProvider`、`NativeTimerProvider`、HostBridge 安装入口和 timer/tick 专用事件分支已删除，crate 的 `std` feature 不再依赖 `vo-ext`，原生 runtime 不再创建 thread-per-request timer。`cargo test -p vo-app-runtime --features std --locked -- --test-threads=1`：73 passed；默认 alloc-only 测试：59 passed；带 `std` 全目标检查通过。
- HostServices V2 的 owner/binding 与 VM storage 已从 `std` 条件下沉到 `alloc`；`vo-runtime` 与 `vo-vm` 的无默认特性检查通过。`cargo check -p vo-app-runtime --target wasm32-unknown-unknown --no-default-features --locked`：通过，首次冷构建 40m47s。
- Studio WASM GUI 入口已切到 hosted AppSession，并公开 `pollHostRequest` 与 `completeHostRequest`：浏览器只看到 RequestId、capability name、deadline、payload 与 terminal outcome，完整 HostWaitKey 留在 AppSession 私有映射；completion 通过 generation-aware signal 唤醒 VM 后执行 `run_scheduled`。独立 `apps/studio/wasm` wasm32 workspace check 通过；native 端同一 command/completion/wake 映射定向测试 1 passed。
- Native event loop 已公开 `on_host_request` 与只含 event sender 的 `NativeHostRequestCompleter`；平台 worker 只能 enqueue terminal outcome，VM owner thread 校验 RequestId 后消费 HostWakeSignal 并执行 `run_scheduled`。缺少 handler 时 Begin/Cancel 分别收敛为 Unsupported/Cancelled。completer owner-thread 定向测试通过，独立 Studio Tauri workspace check 通过。
- schema 已声明 `app.timer.once`，Rust/Vo/TypeScript 生成物共享同一 capability 常量，exact schema fingerprint 与 provenance 已刷新，生成器 `--check` 通过。原生事件循环自动发布该 capability，将严格 8-byte little-endian delay payload 调度到 SessionKernel TimerWheel，只按下一条真实 deadline 使用一次性 `recv_timeout`；idle 无 timer 时继续阻塞 `recv`。到期 completion、HostWakeSignal 消费、VM owner-thread `run_scheduled` 与 render buffering 已串通，取消会收敛 terminal request 并回收内核 timer。
- Studio WASM 自动发布同一 timer capability；浏览器 `setTimeout/clearTimeout` 只充当真实时钟适配器，request ownership、deadline 与 completion 仍经过 AppSession TimerWheel。大于浏览器单次 timeout 上限的 `u64` delay 会分段，取消清理 JS handle 与内核请求，非内建 capability 保留在有序 browser command queue 供 `pollHostRequest` 消费。
- 最新 `cargo test -p vo-app-runtime --features std --locked`：76 passed；`cargo check -p vo-app-runtime --no-default-features --locked`、Studio WASM wasm32 locked check 均通过。事件循环 deadline fake test 覆盖 future/due/overdue 三种 wait 计算；`cargo test -p vo-app-protocol --locked`：9 passed。
- legacy Rust `HostServices` trait/`SharedHostServices`、VM owner 字段与 set/clear/has API、child-island 复制参数，以及 `vo-ext::HostBridge`/provider traits 已删除；仓库当前 Rust 源码只保留 HostServices V2 owner/binding。V2 VM owner tests 5 passed，child-island inheritance 1 passed，runtime ABI/context tests 8 passed，`vo-ext` unit tests 2 passed、doc tests 6 ignored；`vo-runtime` 与 `vo-vm` no-default-features checks 通过。Vogui 仍调用的 timer/tick shim 明确保持 inert，等待 package-level handler registry 随新 framework Session 一起删除。
- 中央 `vo-schema-compiler` 已新增通用 bounded framework schema 编译入口与 Rust/Vo/TypeScript renderer；`vo-dev generate vogui-protocol --write|--check` 通过并按 `eng/project.toml` 的 `local_hint` 定位 owner。Vogui 新增 `vogui.ui` schema、digest provenance、三语言定义与 Rust build-time generated constants；TypeScript no-emit 与生成 Vo package check 通过。
- Vogui 新 `v2` packet header 携带 UiSession/UiRoot generational identity、UiRootEpoch、AppCodeEpoch、revision、sequence 与 bounded payload length，未知 kind、无效 handle、越界和长度不匹配在 payload 暴露前拒绝。旧 decoder 与其测试已退出活动树；clean-slate `vogui-protocol` 当前 1 test passed。
- 新 `vogui-runtime::UiSession` 是实例 owner，包含 UiRoot slot generation、AppCodeEpoch/CommitRev、原子 patch commit、每 root 单 inflight batch、bounded pending accumulator/snapshot fallback、renderer restart epoch，以及有界 UiReturn queue。两个 root 中一个 stalled 时，健康 root 继续提交；ApplyAck 前 Event、stale epoch、late root generation 与重复 root patch 被拒绝。UiRoot slot、renderer epoch 与 AppCodeEpoch 使用 checked generation exhaustion，溢出在状态 mutation 前拒绝。clean-slate workspace 的 runtime 5 tests、protocol 1 test，共 6 passed 且 warning-free。
- Vogui Headless `RetainedTree` 接收短生命周期不可变 ViewNode，先做 node/depth/string/prop/key 全量 preflight，再构建 staging tree并一次提交 UiTransaction。keyed sibling reorder 保持 NodeId 并只发 Move；element type change 分配新 NodeId；duplicate key/capacity/generation exhaustion 不推进 revision；完全相同 View 产生零 patch 且 revision 不变。
- `NodeRef` 只解引用当前 RetainedTree generation。`NodeId + event name` 绑定有界、generation-aware EventToken：存活节点重复绑定保持 token，节点替换后旧 token 失效并以同 index/new generation 复用；event generation exhaustion 会在树替换提交前原子拒绝。每个 UiRoot 现在由 UiSession 直接持有自己的 RetainedTree；UiReturn Event 必须在对应 root token arena 中解析，单凭格式有效的伪造 token 会被拒绝。Vogui clean-slate workspace 当前 runtime 12 tests、protocol 1 test，共 13 passed，warning-free。
- Vogui 生产提交入口已收束为 `UiSession::commit_views`：所有 root 先 prepare RetainedTree，随后执行 deterministic bounded UiTransaction codec 与 patch-byte admission，全部成功后才统一 apply tree、更新 pending presentation accumulator 和 App CommitRev。任一 root duplicate key 或 encoded patch 超限时，其他 root revision/tree 维持原值；全 root no-op 不推进 CommitRev。raw byte patch 与单 root immediate reconcile 仅保留为 `cfg(test)` adapter。当前 runtime 14 tests、protocol 1 test，共 15 passed，warning-free。
- Vogui `UiCommandQueue` 已接入 UiSession：command id 单调且不复用，count/payload/kind 配额有界，poll 只派发一次，CommandResult 与 ApplyAck/Event 共用 root return sequence，duplicate terminal 拒绝。renderer restart 对已派发未终结/尚未派发命令分别产生 OutcomeUnknownOnRendererRestart/DroppedBeforeDispatch；detach 与 AppCode reload 同样归还 terminal 结果。
- 公开节点引用升级为字段私有的 `UiNodeRef { UiRootId, NodeRef }`，只能由 owning UiSession 校验当前 root tree 后创建；event binding 与 command target 均消费 owner-qualified reference。detach/restart/reload 的 generation exhaustion 在清理 command queue 前 preflight，失败时 pending/dispatched command 均保持。Vogui clean-slate workspace 当前 runtime 19 tests、protocol 1 test，共 20 passed，warning-free。
- Vogui `AsyncRegistry` 已由 UiSession 持有：EffectId 单调，request/completion count+byte+label 配额有界，host completion 只进入 completion queue，由 framework safe point 显式 drain；duplicate/late/stale AppCodeEpoch completion 拒绝。Subscription 用稳定 key reconcile，未变化保持 handle，descriptor 变化或删除先 Stop，slot generation checked 后再复用。
- AppCode reload 先 preflight epoch 与 subscription generation，再原子返回 pending effect Cancelled、全部 Subscription Stop 与 command terminal，清理旧 completion，最后推进 AppCodeEpoch；旧 effect completion 随后返回 UnknownEffect。Vogui clean-slate workspace 当前 runtime 23 tests、protocol 1 test，共 24 passed，warning-free。
- Vogui snapshot cut 已从纯 marker 升级为真实 bounded payload：`RetainedTree::snapshot_transaction` 按 parent-before-child 稳定遍历生成完整 Create/SetKind/SetProps replacement transaction，base revision 固定为 0，目标 revision 对齐当前 tree；`poll_presentation` 在独立 per-root snapshot byte budget 内编码并保持单 inflight，超限或 revision 漂移拒绝发送。仅 `cfg(test)` raw-byte adapter 在没有 tree 的旧单元测试中保留空占位，生产入口无法走该分支。最新 clean-slate workspace runtime 24 tests、protocol 1 test，共 25 passed，warning-free。
- Vogui 新增平台无关 `RendererMirror`：对 concatenated UiTransaction stream 执行 payload/patch/node/string/prop 硬预算解码，先校验 UiRoot、UiRootEpoch、AppCodeEpoch、outer base/new revision 与内部 transaction revision chain，再在轻量 mirror staging copy 上应用 Create/Remove/Move/SetKind/SetProps，完整验证单 root 可达树后唯一提交。malformed、wrong owner、base mismatch、父子整体替换失败均保持 last-good tree/revision；新 renderer generation 可从 revision 0 replacement snapshot 恢复。最新 clean-slate workspace runtime 26 tests、protocol 1 test，共 27 passed，warning-free；`vogui-protocol --check` 与 24 REQ/24 TEST/86 ACC traceability lint 通过。
- 用户明确授权 clean-slate 后，Vogui/Voplay 当时源码、tracked 修改、untracked 资产与生成物先封存为仓库外归档 `/Users/macm1/code/github/.rewrite-backups/20260722`；排除 `.git`、`target`、`node_modules`、`.svelte-kit`。Vogui archive 1.1M，SHA-256 `d453545f605db3ebf815b34b842787d3c1b2ec45a41eff78b9d536f1c20c7c24`；Voplay archive 13M，SHA-256 `2a9e2ff70bc13c3b96b3e60a6bdf65b80fd9ca11ee5a3f234d4b94555c84f1e1`；两者通过 `gzip -t`，README 记录原 HEAD。随后旧活动树连同大型构建缓存分别移动到同日期目录下的 `vogui-active-tree/`（11G）与 `voplay-active-tree/`（44G），临时 `voplay-next-build-cache/` 为 117M，操作可逆。当前 Vogui 活动树只含 `protocol/`、`rust/{protocol,runtime}`；Voplay 只含 LICENSE、`protocol/`、`rust/crates/{voplay-protocol,voplay-runtime}`。
- Voplay 新实现已从临时 `next` 提升到计划正式布局：`protocol/` 与 `rust/crates/{voplay-protocol,voplay-runtime}`，活动构建图不再包含旧 package。`voplay.engine` schema 与 generated Rust packet header 携带 Engine generation、channel epoch、RenderCommitId、base/new RenderRevision、required control revision、source simulation revision、sequence 和 bounded payload；protocol test 1 passed。
- 新 `voplay-engine-core::Engine` 为实例 owner；RenderEntity 显式携带 EngineId，底层 EntityId 位级相同也无法跨 Engine 解引用。RenderState transaction 在完整 preflight 后直接应用，不复制整棵 RenderWorld；entity lifecycle/value、channel epoch、commit/base/new revision、control barrier、op/byte quota 同一原子合同。revision 使用 checked successor，`u64::MAX` 不会被饱和加法误收。
- `World` 新增显式 WorldId owner、代际 Entity slot、稳定 key、结构命令 preflight、原子 stage apply 和实际变化 journal；WorldEntity 同时携带 WorldId 与 EntityId，跨 World/late generation 均拒绝。相同值 upsert 不推进 World revision。SimulationClock 只混合 tick、World revision 和 InputFrame，presentation notification 不进入 deterministic hash。
- RenderEvent 按 min render revision 与 required control revision 双 barrier staging；同一 front event 派发一次后不会重复交付。renderer restart 对未 dispatch/已 dispatch 未终结事件分别生成 DroppedBeforeDispatch/OutcomeUnknownOnRendererRestart。
- Voplay `Schedule::configure` 已实现固定 stage 顺序、稳定拓扑排序与 schedule hash、依赖 cycle/未知依赖/逆 stage 约束、未排序读写冲突检测；Extract/Frame 写 SimulationWorld 和 deterministic tick 读取 PresentationState 在 Configure 阶段拒绝。相同 systems 的注册顺序不影响 order/hash。
- Voplay `SparseStore<T>` 已实现有界 sparse slot + dense values/entities、O(1) lookup/add/remove、swap-remove 索引修复、added/changed/removed journal 和独立稳定 Entity 顺序。no-op upsert 不推进 store revision；WorldId、Entity generation、同 slot generation 碰撞及超配额伪造 index 均在 mutation 前拒绝。
- WorldId 已从裸 Handle 升级为 `EngineId + World Handle`；WorldEntity 因此完整携带 Engine/World/Entity 三层 owner。两个 Engine 分配相同 World slot 和 Entity slot/generation 时仍相互拒绝；SparseStore 同时校验 Engine 与 World owner。
- `EngineControlStore` 已建立 producer/logic-generation 绑定且有容量的 allocator lease。ProvisionalControlRef 保持 builder-private，commit 只返回不可直接读取 promotion 的 ACK；匹配 Logic generation 的 safe point 才产生公开 StableControlRef。Render/Audio revision 分离，desired/realizing/live/tombstone 状态、transient/permanent realization、endpoint generation restart 后用同一 stable ref 重建，以及跨 Engine 拒绝均有定向测试。
- control ref 删除 transaction 携带 RenderRevision、domain Frame、RenderEvent 与 AudioEvent 四维 retirement fence；全部 terminal 进度达到 fence 前 `retirement_ready` 保持 false。descriptor byte 累加、u32 handle capacity 和 transaction/ref/lease 配额均显式有界。
- `InstanceGroupSupervisor` 是 role lifecycle/endpoint generation/actor affinity 的串行 authority，并持有同一 EngineControlStore。wrong Engine/role/actor、stale endpoint 全部拒绝；Render fault 更换 generation、保持 Logic endpoint 与 desired control descriptor，并用同一 StableControlRef 进入重建。未启用兼容 snapshot policy 时 Logic fault 使 group Failed；Closing 等待全部 role terminal Closed。
- World Entity generation、role endpoint generation 与 renderer channel epoch 均使用 checked exhaustion；溢出不回绕，且在 World/RenderWorld/event mutation 前保持 last-good state。两套 SimulationClock 使用相同 10,000 个 InputFrame/World revision、不同 presentation pulse 密度后得到同一 deterministic hash。Voplay 新 workspace 当前 runtime 22 tests、protocol 1 test，共 23 passed，warning-free。
- Logic 侧 `RenderOutbox` 已实现单 inflight RenderState commit、跨 tick bounded compact accumulator、ACK 后 revision/world baseline、错误 ACK 保留 inflight、稳定投影零 packet、delta 预算超限 snapshot cut，以及独立 snapshot object/byte budget。批量 enqueue 在中途 Entity 错误时不留下前序 op；producer transaction 已与 renderer apply/ACK 做同测。
- `PresentationDomainId` 显式携带 Engine owner；每 domain 只保留最新 RenderTransient。消费同时检查 required RenderRevision/RenderControlRevision，未达到 barrier 时保留 slot，其他 ready domain 继续消费。Voplay clean-slate workspace 当前 runtime 28 tests、protocol 1 test，共 29 passed，warning-free。
- Voplay `InputDomain` 已实现 Engine-owned InputScope registry、normalized event/device generation、全局 admission sequence、event/count/byte/scope/binding 配额与 tick-monotonic capture。Vogui 消费的事件只推进 anti-replay admission sequence，不进入 scope queue、ActionMap、`through_sequence` 或 deterministic frame bytes；不同 scope 独立 drain，ActionSample 按稳定 action id 输出。Voplay clean-slate workspace 当时 runtime 32 tests、protocol 1 test，共 33 passed，warning-free。
- RenderOutbox 的 snapshot cut 已接入真实恢复链路：`prepare_snapshot` 持有单一 in-flight compact snapshot，renderer 按独立 object/byte budget 完成全量 preflight 后以 move 替换 RenderWorld，只有 exact commit/revision ACK 才推进 producer baseline 并解除 snapshot-required。错误 ACK 保留 snapshot inflight；renderer 与 outbox 同步换 channel epoch 后可从最新 snapshot 恢复，再继续 base/new revision delta。malformed/foreign-Engine snapshot 保持 renderer last-good world/revision。最新 Voplay workspace runtime 35 tests、protocol 1 test，共 36 passed，warning-free。
- Voplay 新增 World-owned `Hierarchy`：节点 identity 完整包含 Engine/World/Entity generation，insert/reparent/delete 均先验证 owner、容量、深度、cycle 与 transform overflow。`KeepLocal` 原子更新整棵子树 world transform，`KeepWorld` 重算 root local transform；删除支持整棵递归移除或将直接子节点 keep-world 提升到父节点。失败路径保持 parent/children/local/world last-good 状态。最新 Voplay workspace runtime 38 tests、protocol 1 test，共 39 passed，warning-free。
- Voplay `ComponentPresenceIndex` 与 `QueryPlan` 已建立 generated store/query 的非泛型增量核心：presence 只消费 SparseStore journal，按 component revision 防止 stale/duplicate replay，journal owner/sequence/presence/capacity 任一失败时不提交部分结果。QueryPlan 预编译 required/excluded component set，从最小 required set 做稳定交集，完整校验 World owner，不扫描 World entity arena。最新 Voplay workspace runtime 41 tests、protocol 1 test，共 42 passed，warning-free。
- Voplay `GameEngine` 已把 target-island `GameEntryDescriptor`/factory construct、Configure→Schedule、World、SimulationClock 与 InstanceGroupSupervisor 组合为单一实例生命周期。descriptor role set、actor set 与 supervisor role set 在构造 Game 前 exact 校验；每 role 只通过 supervisor start/ready/close。embedded manual-step 与 headless runner 走同一 construct/start/tick/shutdown 状态机，presentation pulse 独立执行 Extract/Frame，10 tick 对照得到相同 deterministic hash，且 factory 只构造一次 Game。最新 Voplay workspace runtime 43 tests、protocol 1 test，共 44 passed，warning-free。
- `EngineControlStore::snapshot` 已为 Render/Audio domain 生成独立、总 descriptor byte 受限的 desired-state snapshot，携带 Engine/domain/revision/last transaction/stable Ref/descriptor/tombstone fence；Desired/Realizing/Live 统一投影为 Desired，因此 endpoint-local realized handle 不进入恢复数据。snapshot budget 失败不改变 control state。
- `InstanceGroupSupervisor` 对 Render replacement generation 强制 `ControlSnapshot → RenderStateSnapshot → AssetResidency → Ready` 恢复状态机：control ACK 必须 exact 当前 control revision，RenderState 的 required control revision 不得越过已应用 control snapshot，Asset residency 必须匹配该 RenderState revision；跨阶段、错误 revision 和提前 Ready 均拒绝。动态 RenderView stable Ref/descriptor 跨 endpoint generation 保持。最新 Voplay workspace runtime 45 tests、protocol 1 test，共 46 passed，warning-free。
- Audio replacement generation 采用对称的 `AudioControlSnapshot → AssetStreams → DeviceActivation → Ready` 顺序；asset stream rebind 声明的 required AudioControlRevision 不得越过已应用 snapshot，device activation 无法跳过 stream 阶段，提前 Ready 拒绝。AudioBus stable Ref/descriptor 跨 endpoint generation 保持，realized state 回到 Realizing 等待新 generation 实现。最新 Voplay workspace runtime 46 tests、protocol 1 test，共 47 passed，warning-free。
- Voplay 新 `AudioEndpoint` 持有 Engine/endpoint generation、ReadyLocked/Active/Suspended/Lost 状态与独立 AudioEvent lane。one-shot 携带 Engine、EventSequence、EventId、TickId、required AudioControlRevision、deadline 和 payload；count/byte admission 原子且失败不消耗 sequence。ReadyLocked 默认返回 terminal AudioLocked，也可按配置有界 defer；control barrier、deadline terminal、gesture identity、device state、restart 的 DroppedBeforeDispatch/OutcomeUnknownOnAudioRestart 和 persistent source ResumeTimeline/Restart/StopOnRecovery 策略均有定向测试。
- RenderEvent 同步升级为 Engine-owned EventId/sequence/deadline one-shot；poll 同时等待 min RenderRevision 与 required RenderControlRevision，deadline 与 control unavailable 生成 terminal result，wrong Engine/byte quota 失败不留下 admission 状态。renderer restart 继续对 dispatched/undispatched 合成 OutcomeUnknownOnRendererRestart/DroppedBeforeDispatch，且 event_id 保持。最新 Voplay workspace runtime 50 tests、protocol 1 test，共 51 passed，warning-free；Voplay protocol generation check 与 24 REQ/24 TEST/86 ACC traceability lint 通过。
- RenderEvent terminal 路径进一步加入有界 completion window：多个 barrier-ready event 可以继续 dispatch，terminal 可乱序进入以 EventSequence 为键的窗口，`drain_contiguous_event_results` 只越过从 ACK cursor 开始的连续 sequence；restart 将已有 terminal 与 dispatched/undispatched 合成结果合并后再连续排出。Render/Audio EventSequence admission 均要求严格无间隙，capacity 或 identity 失败不消耗序号。Voplay workspace 仍为 runtime 51 tests、protocol 1 test，共 52 passed，warning-free。
- World 新增 schema-versioned、World-owned、总字节受限的 snapshot：完整保存 WorldRevision、每个 slot generation/live stable key/components 与 free-list 顺序，因此 restore 后 stale handle 语义和下一次 allocation 顺序保持。restore 只允许写入空的新 World，先验证 schema/owner/slot/free/stable-key/component/capacity 后一次提交；malformed/cross-World snapshot 保持 target 空状态。SimulationClock snapshot 只保存 TickId 与 deterministic hash，明确排除 presentation notification；restore 后继续 tick 得到同一 hash chain。最新 Voplay workspace runtime 54 tests、protocol 1 test，共 55 passed，warning-free。
- Voplay Asset foundation 新增 AssetId/ArtifactId、跨 role/restart 稳定 AssetRef、AssetEndpoint-local AssetNodeHandle、AssetScope 与异步 AssetTicket。批量注册和 hot reload 在 mutation 前验证 identity/dependency quota/unknown dependency/全图 cycle；hot reload 更新 source/artifact revision 并保持 AssetRef。并发 Ticket 共享一次 CPU ready completion，worker 只经 poll 获取 work；cancel/timeout/scope close/shutdown 全部 terminal。Asset endpoint generation 变化后 dispatched work 重新排队、旧 NodeHandle 和旧 completion 被 StaleEndpoint/LateCompletion 拒绝。最新 Voplay workspace runtime 58 tests、protocol 1 test，共 59 passed，warning-free。
- per-device `ResidencyStore` 将 AssetRef/ArtifactId 与 `ResidencyKey(Device generation + usage/format/quality)` 分层，持有 endpoint-local generation handle 和 UploadQueued/Uploading/Ready/Failed/Evicted 状态。artifact hot reload 推进 residency handle generation 并拒绝旧 upload completion；Render endpoint restart 原子推进 endpoint/全部 local handle generation 并 budgeted requeue。DeviceLost 只 Evict 精确 DeviceId+generation，CPU asset 与其他 device Ready residency 保持。最新 Voplay workspace runtime 60 tests、protocol 1 test，共 61 passed，warning-free。
- `BufferLeaseRegistry` 为 cooked artifact 跨 role 传递建立 Engine/provider generation/consumer/lease generation/ArtifactId/length/deadline exact binding，同时限制 live lease、总字节和单 chunk 字节。`map_span` 的借用无法越过 registry borrow，`read_chunk` 才执行显式 bounded copy；owner、provider、digest、deadline、range 在 byte exposure 前验证。release/cancel、consumer close 与 provider restart 回收 byte budget 并推进 generation，late handle fail-closed。最新 Voplay workspace runtime 63 tests、protocol 1 test，共 64 passed，warning-free；Voplay protocol generation check 和 24 REQ/24 TEST/86 ACC traceability lint 通过。
- Voplay `RenderGraph` compiler 新增 bounded resource/node/access/label schema，先规范化声明顺序，再执行 stable topological sort、unknown dependency/cycle 与未排序 read/write hazard 检查。resource 从 version 0 开始，read version 和 write base version exact 校验；同 node feedback、duplicate access、format/usage、transient present/readback、external-surface lifetime 均在 allocation 前拒绝。编译后按真实 first/last use 为 format/sample/usage 相同且 lifetime 不重叠的 transient resource 分配 alias slot，persistent/external 保持独立，并生成与注册顺序无关的 signature。最新 Voplay workspace runtime 66 tests、protocol 1 test，共 67 passed，warning-free。
- Voplay 新增 typed `RenderTargetDescriptor`/`RenderViewDescriptor` 与 `RenderControlBuilder`：size/format/depth/sample/usage/lifetime/clear、camera/domain/viewport/layer/graph/quality 在进入 store 前验证并采用显式稳定 wire tag。provisional target/view 类型保持 builder-local opaque；跨 builder 引用在本地拒绝且不消耗 transaction capacity。EngineControlStore commit 预检 stable/provisional descriptor dependency，原子分配全部 promotion，并把 descriptor 内 provisional binding 改写为 stable Ref 后才写入 desired state，因此 endpoint 重建和 control snapshot 不携带失效 token。跨 Engine、错误 kind 与同 Engine 伪造 handle 均在 mutation 前拒绝；Voplay workspace runtime 71 tests、protocol 1 test，共 72 passed。
- AssetServer 的 terminal ticket 与 closed scope 新增显式 release：terminal result 在消费前仍可检测 duplicate terminal，消费后归还并发 ticket quota；closed scope 仅在所有 ticket record 释放后归还 scope quota。定向测试覆盖容量满、active ticket 错误 release、terminal ticket 回收、scope in-use 与重复创建；World 同时拒绝超过 u32 handle domain 的 entity 配置，并以 checked multiplication 计算 snapshot free-list bytes。上一轮 workspace 中其余 71 项通过，新增配额回收定向测试通过。
- Voplay 新 `PhysicsRuntime` 以 `WorldId` 为 owner，并以同一裁剪接口承载 2D/3D space；Body/Collider/Joint 均使用 generational handle，销毁复用后 late handle fail-closed，带 Collider 或 Joint 的 Body 无法提前销毁。Joint descriptor 支持 Fixed/DistanceLimit、body owner、`collide_connected` 与 break threshold，并完整进入 bounded snapshot/free-list restore。reference solver 对 Fixed 执行动态端 exact correction，对 DistanceLimit 使用稳定主轴逐步收敛；constraint applied/broken event 绑定 exact Tick，event quota、generation exhaustion、constraint overflow 与后续 contact 在同一 staging transaction 成功后才提交。PrePhysics command batch 明确 static/kinematic/dynamic 的 World target、kinematic target、teleport、impulse、velocity owner policy；fixed step 产生 layer/mask/sensor contact。point query 与 batch ray/shape cast 只读取 exact Tick snapshot，cast 使用整数 slab 和 exact rational TOI，以 ColliderId 作为稳定 tie-break，并限制 query count 与总 hit count。actor-affine `PhysicsBackendAdapter` 将同步 batch call 绑定 World、backend generation 与 Logic actor；`PhysicsStageBinding` 绑定 frozen Schedule hash 和具名 PrePhysics/Physics/PostPhysics system，`PhysicsStageAdapter` 强制阶段单向推进、step failure 原阶段 retry、Idle-only backend restart。成功 PostPhysics frame 稳定携带 pose/contact/joint event。最新 Voplay workspace runtime 84 tests、protocol 1 test，共 85 passed，warning-free；外部高保真 2D/3D provider、旋转/角速度/复杂 shape 与真实 backend differential 仍待后续切片。
- Voplay 新 `SceneInstance` 明确分离 authoring/runtime identity：`AuthoringObjectId(SourceAssetId + LocalObjectId)`、有界 `PrefabInstancePath`、`RuntimeObjectKey(RootSceneInstanceId + path + authoring)` 与 generational Entity 各自独立；同 prefab 多实例和 nested placement 得到唯一 key/entity。SceneAsset object 只含 field map、authoring refs、partition 与 override，不含 runtime Entity。`PrefabRelative` 结合当前 path、`SceneLocal` 固定 root path、`ExternalBinding` 要求显式 visible binding；expected type、required/optional、field identity 和 path quota 在 staging resolve，optional unresolved 形成 diagnostic，required/type/visibility 失败不触碰 World。全量 reconcile 先验证 duplicate key/stable-key collision、override、component/field/ref count 与 checked encoded byte size，再将每个 scene object 编成单一保留 World component；World `apply_stage` 成功后才替换 `RuntimeObjectKey → Entity` mapping。partition unload/reload 保持存活 key 的 Entity，重新载入 key 获得新 generation；mapping revision 仅在 key/entity mapping 变化时推进，cached Entity 必须携带 exact revision。World capacity、mapping exhaustion、invalid override/type migration、foreign component mutation均在双 owner 提交前失败。Scene recovery snapshot 在独立总 byte budget 内保存 source/mapping revision、key/entity、object schema version/blob、resolved refs、diagnostic 与 AssetScope phase；restore 只写空 SceneInstance，并逐项对照已恢复 World 的 stable key、Entity generation 和保留 component bytes，malformed/cross-owner/drift 保持 target 为空。scene-local AssetScope attach 只接受同 Engine 的 open Scene scope；close 先使全部 ticket terminal，terminal record 释放完毕后才 release scope，Open/Closing phase 可随 snapshot 恢复并对照 AssetServer。`PrefabRegistry` 对 batch registration 与 hot reload 统一执行 source/revision、依赖配额、unknown dependency 和全图 DAG 校验；cycle、stale revision 与未知 prefab source 在 Scene/World mutation 前原子拒绝。Scene object type 或 schema version 变化必须经过显式 `SceneMigrationProvider`，迁移失败及 provider 违反 key/type/schema/partition 契约时保留原 Scene/World；成功迁移保持原 Entity 并提交新 schema blob。最新 Voplay workspace runtime 94 tests、protocol 1 test，共 95 passed，warning-free；外部 authoring import pipeline 与真实 schema provider ABI 仍待后续切片。
- Voplay 新 `AnimationRuntime` 提供 World-owned、generational `AnimationMachineId` 与固定 Tick simulation animation。Machine descriptor 对 state/transition identity、target state、parameter/event identity及容量做全量预检；每 tick command batch 先在 staging slots 应用，duplicate command、跨 World、parameter/event capacity 和 root-motion 算术溢出均保持 tick、machine state 与累计位移不变。transition 从当前 state、minimum ticks 与 parameter condition 中按 priority 降序、transition id 升序稳定选择；成功迁移可产生绑定 exact Tick/machine/entity/transition 的 gameplay event，并在同帧应用目标 state 的 root-motion delta。snapshot 在独立 byte budget 内保存 slot generation/free-list 顺序、descriptor、state ticks、parameters 与累计 root motion；restore 只写空 runtime，malformed/free-list drift 被原子拒绝，恢复后相同 command 得到逐字节相等的 frame 并保持下一次 handle allocation。Render endpoint 侧 `PresentationAnimationEndpoint` 复用 Engine-owned AssetRef 和 WorldEntity，animator handle 同时绑定 endpoint generation 与 slot generation；descriptor 显式持有 skeleton、graph、clip、mask、mesh 和 material 依赖。每帧请求校验 clip binding、插值分数、重复 animator、required RenderControlRevision 与 palette byte budget；skeleton/graph/当前 clip/mask 必须在真实 AssetServer 达到 CpuReady，mesh/material 必须在相同 Render endpoint generation 和请求 ResidencyKey 上达到 Ready。provider 公开 Engine/generation/Ready-Closing-Closed lifecycle，graph artifact 求值得到有界、按返回顺序执行的 blend layer plan；每层 clip/mask 必须来自 descriptor，逐骨骼 u16 mask 与 layer rational weight 组合后执行 checked 定点混合。provider 按 previous/current/layer clip+tick 采样 exact bone count，最终生成 bone palette 和带 mesh/material 的 GPU skinning dispatch descriptor。不可见 animator 在 provider、CPU asset 和 GPU residency admission 前 cull，允许 Reloading/Evicted/Closing 状态产生零 palette/dispatch；control barrier、CPU hot reload、device loss、provider Closing/restart、graph capacity/unbound dependency、provider contract 和预算失败均不推进 presentation revision。2D `SpriteAnimationSystem` 同样绑定 Engine/Render endpoint generation/WorldEntity/AssetRef，支持变长 frame 的 Loop、Once、PingPong、显式 clip switch 和单次 completion；每 animator 保存独立 presentation pulse anchor，暂时未提交的 animator 下次请求时可按完整 elapsed time 追赶。pulse batch 在 staging slots 上原子推进，time regression、transition/completion capacity、control barrier、CPU sheet 和 texture/material residency 失败不改变 revision 或时间线；culled sprite 继续推进 animation state且不要求 ready asset、不产生 draw packet。`AnimationGpuAdapter` 将 3D bone palette 与 2D sprite draw 合并为单一 Engine/Render generation/actor-affine submission，在 backend 调用前重验 required control revision、每项 ResidencyKey、packet shape、command/palette/encoded byte budget。完整 command 值排序使输入 packet 排列不影响 bytes；schema/count/边界 parser 与稳定 digest 在 provider 侧形成独立 differential。backend pre-submit reject 保持同 submission 可重试；OutcomeUnknown 或任一 ACK 字段不匹配进入 RecoveryRequired，禁止静默重放且不推进本地 revision。最新 Voplay workspace runtime 116 tests、protocol 1 test，共 117 passed，warning-free；具体 native wgpu/browser WebGPU backend 接入及跨后端命令/像素 differential 仍待后续切片。
- Voplay 新 `WorldPartition` 建立 chunk manifest DAG、显式 desired-set dependency closure、稳定 dependency-first load order，以及 active partition/object/byte 三维预算。manifest batch 在提交前统一拒绝 unknown dependency、cycle、duplicate identity 和 dependency/asset capacity。`AssetServer::create_scope_with_requests` 为单 partition 原子创建 Scene AssetScope 与全部 ticket；scope/ticket/identity/generation 任一 preflight 失败不消耗 allocator 或 lease。partition 按 Unloaded→Loading→Ready→Active 状态推进，全部 AssetTicket terminal Ready 后才允许安装 chunk；object partition identity、manifest object/byte estimate 在 install 时重验。activation 按稳定 PartitionId 合并对象与 override，并以单一递增 SceneTransaction 调用现有 Scene/World 双 owner原子提交；Scene duplicate/reference/capacity 失败保持 partition state、source revision、Scene mapping 和 World 不变。desired closure 缩小时先以 Scene transaction 移除对象，再关闭 partition scope、释放 terminal ticket/scope；重新加载同 RuntimeObjectKey 得到相同 entity slot 的新 generation。空间 planner 使用 checked AABB interest volume、独立 load/unload margin 和 resident-aware hysteresis；边界范围内的 Ready/Loading/Active chunk 保持 desired，越过 unload margin 才进入 dependent-first cleanup。多个 interest 的 manifest+interest priority 与 AABB distance 形成稳定排序，候选 dependency 继承最高 dependent priority；active 三维预算采用贪心稳定选择并报告 `budget_excluded`，并发 load slot 之外进入 `deferred_load`。failed dependency 阻断整个 dependent closure并报告 `blocked_by_failure`，cleanup 按 dependent-first 排序。相同输入在 manifest 反向注册后仍产生逐项相等的 StreamingPlan。`PartitionIoScheduler` 将 File/Http source、priority、deadline、max bytes、optional digest、stable request handle 与 provider generation 分离；Logic 只 poll `PartitionIoWork`，worker completion 在 byte exposure 前校验 actor、provider/request generation、source、length、deadline 和 digest。真实 `NativeFilePartitionIoWorker` 使用 capped reader 从临时文件读取并拒绝 oversize；Http source 通过同一 worker trait 留给平台 provider。pending cancel 立即 terminal，dispatched cancel 等 worker completion；Closing 将 pending 置 Closed、标记 dispatched cancel，`finish_close` 在所有 dispatched terminal 前返回 DrainPending。provider restart 把 dispatched 请求重排队并拒绝旧 generation completion；terminal result 消费后归还 byte budget并推进 request generation。新增版本化 `VPC1` PartitionChunk codec：编码按 RuntimeObjectKey 与 override identity 规范排序，完整覆盖 object/component/field/value、PrefabRelative/SceneLocal/ExternalBinding 三类 authoring reference 及 prefab override；解码在构造 chunk 前检查 schema version、总字节、对象/组件/字段/引用/覆盖/path/value 配额、重复 identity、authoring identity、partition 与 root scene owner。`WorldPartition::install_io_result` 只接受摘要已验证的 Ready I/O 结果，并将 expected partition/scene 一并传入 decoder 后原子安装 chunk。真实临时文件端到端测试覆盖 encode→native capped read→digest verification→decode→install→Scene/World activate→live entity。最新 Voplay workspace runtime 132 tests、protocol 1 test，共 133 passed，warning-free；具体 HTTP client provider、schema N↔N-1 演进与 fuzz、大规模 streaming 压力测试仍待后续切片。
- Voplay 新 `AudioStreamingEndpoint` 将 stable `PersistentAudioSource` 与 endpoint-local `AudioStreamHandle` 分离，stream handle 同时绑定 Engine、Audio endpoint generation 和 slot generation；销毁复用及 endpoint restart 后的旧 handle 均 fail-closed。每个 stream 在创建时按声明 frames×channels 预分配 PCM ring，endpoint 对 stream 数、总保留 samples、channel、单次 decode frames 与 callback frames 分别设硬配额，并以逻辑 ring 容量约束 admission，容器实现的额外 capacity 不进入可用预算。decode worker 只接收有界 `AudioDecodeWork`；completion 在 PCM 暴露前重验 Engine、endpoint generation、decode actor、stream generation、request sequence、start frame、channel shape、frame count 和 ring 剩余容量，伪造或畸形 completion 保留 pending work 与空 ring。callback `render_into` 只向调用方预分配 slice 写入、从预分配 ring 消费并以零样本补齐 underflow，不访问 World/VM，也不发起 decode；report 明确 rendered/silence/buffered/transport frame。looped stream 只在已验证 EOF 后把下一次 decode cursor 回卷到零。endpoint restart 按 stable source 排序产生恢复计划，`ResumeTimeline` 从 transport anchor 恢复 decode cursor，`Restart` 从零开始，`StopOnRecovery` 不重建；所有 endpoint-local ring/work 被清除。最新 Voplay workspace runtime 136 tests、protocol 1 test，共 137 passed，warning-free；具体平台 decoder/AudioDevice callback 接入、bus mixer/3D spatialization 与长时间实时压力/differential 仍待后续切片。
- Voplay 新 `AudioMixerEndpoint` 接收独立 revision 的 typed `AudioControlState`，在替换状态前统一验证 Engine owner、stable AudioBus/PersistentSource kind、bus/source/ducking 配额、gain、listener/spatial 参数、parent 存在性、bus DAG、source→bus 和 duck trigger/target；失败保留旧 revision、拓扑和 live voice。bus 支持 parent hierarchy、Q15 volume、mute、solo；ducking 以 audible trigger subtree 驱动 target subtree gain。persistent source 的 bus/gain/spatial 随新 control revision 在安全点原子刷新，仍被 one-shot/persistent voice 使用的 bus/source 无法提前移除。one-shot 与 persistent voice 均使用 Engine+Audio endpoint generation+slot generation 的 `AudioVoiceHandle`，slot 复用和 endpoint restart 后 late handle fail-closed；restart 清除 endpoint-local voice 并保留 stable control state/revision。`mix_stereo_into` 先完整预检 output frames、input shape、重复 voice、handle generation、spatial mono 限制与 transport 算术，再直接写调用方预分配 stereo slice；每个 frame 用 i64 汇总后统一 clamp，避免输入排列和逐项饱和导致结果漂移，callback 路径不分配、不访问 World/VM。3D source/listener 使用整数距离衰减和 listener right-axis Q15 pan，mute/solo/ducking/层级 gain 与空间 gain 在同一确定性定点链中组合。该轮 Voplay workspace runtime 140 tests、protocol 1 test，共 141 passed，warning-free；真实 AudioDevice callback/platform decoder、bus meter/effect、HRTF 与长时间 realtime differential 仍待后续切片。
- Voplay 新版本化 `VAC1` Audio control descriptor 与 `AudioControlBuilder`：bus descriptor 编码 parent、Q15 gain、mute/solo、唯一 root listener 和 trigger-owned duck targets；persistent source descriptor 编码 stable AssetRef、bus、gain、loop、恢复策略、transport anchor 与可选 spatial source。所有 bus parent/source/duck target 都登记为 `EngineControlStore` dependency；同 builder provisional ref 在 commit 内原子提升为 stable AudioBus binding，跨 builder token、伪造 stable ref 和跨 Engine 引用在 revision 提交前失败。Audio domain snapshot decoder 对 domain/Engine/revision、entry/总 descriptor byte、重复 stable handle、VAC1 tag、exact EOF、binding kind/generation、root/listener 唯一性、bus DAG、source/duck dependency 与各字段配额执行严格校验；tombstone descriptor 同样计入 byte budget且不进入 desired state。成功 decode 得到唯一 typed `AudioControlState`，同一状态直接应用到 `AudioMixerEndpoint`，persistent source 的 loop/recovery/transport anchor 同时初始化 `AudioStreamingEndpoint` decode cursor。端到端测试覆盖 EngineControlStore commit→provisional promotion→snapshot→VAC1 decode→Mixer apply→Streaming anchor，并覆盖 malformed、owner、quota、跨 builder 和 forged dependency 原子失败。最新 Voplay workspace runtime 143 tests、protocol 1 test，共 144 passed，warning-free；真实平台 AudioDevice/decoder 适配与 realtime differential 仍待后续切片。
- App Runtime 新增 session-owned `AudioDeviceLeaseRegistry`，并在轻量 `vo-app-protocol` 共享 `AudioDeviceFormat`、lease/device generation、realtime endpoint identity 与 `AudioDevicePermit`，避免 Voplay callback 依赖 VM/module/HostServices 实现。lease 签发要求同 session 的 Ready AudioControl+AudioRealtime endpoint；容量、format、endpoint 独占和 role 在分配前校验。permit 只在 Active 签发；Active↔Suspended、Active/Suspended→Lost、Lost→Active recovery 均为 typed transition，recovery 必须绑定 replacement AudioRealtime endpoint 并推进 device generation，旧 permit fail-closed。lease 纳入 `SessionKernelLimits`、live count、endpoint close 与 session close 的统一资源回收；Session `begin_close` 同步把全部 lease 转入 Closing，使 realtime permit 立即失效，`finish_close` 返回带 stable identity 的 Closing binding 并释放配额。AudioRealtime 继续无法调用通用 HostServices。Voplay 新 `AppAudioDeviceAdapter` 同时绑定 Engine、Voplay Audio endpoint generation 和 exact App Runtime permit；callback 在写输出前重验 lease/realtime/device generation/format、固定 frame shape 与 mixer generation，rebind 只接受同 lease 的更高 device generation并重置 callback sequence。新 `NativePcm16WavDecoder` 使用 capped native file read 严格解析 RIFF/WAVE chunk、PCM16 format、byte rate/block align、channel/sample-rate、data quota、start frame 与 EOF，再产生绑定原 `AudioDecodeWork` owner/generation/request 的 completion。真实临时 WAV 端到端测试覆盖 file decode→预分配 ring→mixer→permit-bound callback，并与 reference mixer 做逐样本 differential；malformed、oversize、channel mismatch、stale permit 和 recovery generation 在 PCM/output 暴露前失败。最新 root `vo-app-protocol` 9 tests、`vo-app-runtime` 81 tests；Voplay runtime 146 tests、protocol 1 test，共 147 passed，warning-free。具体 CoreAudio/WebAudio/平台设备 provider、压缩 codec provider 与长时间 realtime hardware differential 仍待后续切片。
- App Runtime 新 `CompositionRegistry` 以 SessionHandle+epoch 为 owner，并在 `vo-app-protocol` 共享 Window/View/Surface generational handle identity。Window→View→Surface 生命周期采用硬配额和代际复用，仍被下层对象引用的 owner 无法提前关闭；每个 View 的 Surface 按 `(z-order, handle identity)` 稳定排列，并显式区分 Game/Ui/Diagnostics 与 Observe/Passthrough/Interactive/Exclusive 输入策略。focus、pointer capture 与 IME composition mutation 携带 exact composition revision；仲裁按顶层到下层执行且写入有界 trace，UI text focus 不向 game surface 交付 keyboard，透明 Observe HUD 可接收观察副本并让 pointer 命中下层 game。Surface close 原子合成 ImeComposition/PointerCapture/Focus release，恢复下一层可交互 focus，旧 handle 在 slot 复用后 fail-closed。`SessionKernel` 现在直接拥有 composition limits/registry 并封装全部 mutation；Starting/Running 才能创建，Running 才能仲裁或改变 focus/capture/IME，Suspended 只允许显式关闭，Closing 后业务入口统一返回 `SessionClosing`。`finish_close` 先做 revision/generation preflight，再按稳定 View/layer 顺序释放全部 Surface、View、Window，将 typed `CompositionShutdownReport` 纳入 session close report 并证明 live count 全部归零。composition 定向 5 项、kernel owner/shutdown 1 项及 App Runtime 全量 87 tests、`vo-app-protocol` 9 tests 通过；root all-targets、Studio wasm32 target 和 Studio Tauri all-targets check 通过，warning-free。真实 WebView DOM+WebGPU 与 native UiSurface+wgpu compositor adapter、平台 focus/IME smoke 仍待后续切片。
- Voplay 新 `InspectionSchemaRegistry` 以 Engine owner 管理 component/field schema，统一验证 component/type/version、field identity/name/offset/width/editability、字段重叠和 schema/field/byte 配额；freeze 后禁止变更，并按 component/field 的规范顺序生成与注册顺序无关的稳定指纹。`InspectionService::inspect_world` 读取 exact World snapshot revision，在 entity/byte 双预算内按 slot cursor 输出 owner-qualified generational entity、stable key 与 component bytes。字段编辑 transaction 绑定 Engine、单调 transaction id 与 exact expected WorldRevision，先对 schema、editable、width、重复 edit、component shape 和历史预算做完整 staging，再通过一次 `World::apply_stage` 原子提交；undo/redo 只接受各自记录的 exact revision，外部 World mutation 会产生 revision conflict 且保留历史和 World。独立 control revision 提供 Running↔Paused 与有界 pending step queue，resume 只在 step 排空后成功。定向测试覆盖 schema 指纹/冻结/重叠、分页 owner/revision/cursor/byte budget、编辑/撤销/重做/外部冲突/read-only 原子性、pause/step/resume/capacity；最新 Voplay workspace runtime 150 tests、protocol 1 test，共 151 passed，warning-free。Studio panels、pick/gizmo、frame capture、isolated play mode、真实 compositor 接线与插件集成仍待后续切片。
- Voplay 新 `EditorInspectionService` 为 hierarchy、assets、schedule、render、performance 五类 editor panel 提供统一 Engine owner、source revision、expected revision、entry/byte 双配额和无跳项 cursor 分页。Hierarchy owner 新增按 WorldEntity 稳定顺序导出的 parent/local/world/child-count snapshot 与内容指纹；AssetServer 新增按 AssetId 稳定顺序导出的 stable AssetRef/type/source revision/artifact/dependency/state/lease-count snapshot，依赖顺序规范化后进入内容指纹。Schedule 直接以 frozen hash 和 compiled system order 为 revision/order；Render 以 graph signature 为 revision，依次暴露 compiled node order、final resource version 与 alias allocation；跨 Engine owner 在读取前拒绝。Performance panel 只接收同 Engine、严格递增 sequence 和有界 label 的 sample，达到容量后执行 latest-window 淘汰并显式累计 `dropped_before`，duplicate/out-of-order 不改变窗口。分页在下一 entry 超出 byte budget 时保留该 cursor 给下一页，单 entry 超限返回 typed error。定向 4 项与 Voplay workspace runtime 154 tests、protocol 1 test，共 155 passed，warning-free；Vogui editor Views 的 schema binding、交互 command、pick/gizmo/frame capture 与真实 Studio panel smoke 仍待后续切片。
- Voplay 新 `EditorToolService` 以 Engine owner 管理单 World selection revision、pending pick 和 bounded frame-debug captures。selection replacement 在提交前校验 expected selection revision、World owner、live Entity 与数量，规范化为稳定 Entity 顺序；pick request 绑定 request id、exact WorldRevision、compiled RenderGraph signature 与像素位置，completion 在消费 pending request 前重验 World/render revision、candidate count 和 live owner，并按 depth→Entity generation identity→primitive 稳定选取最近对象。stale completion 保留 pending record供显式 cancel，取消后立即归还配额。gizmo translation 同时绑定 selection revision 与 WorldRevision，检查 component/field identity、三个不重叠 8-byte axis range、component shape 与 i64 overflow，只构造标准 `InspectionEditTransaction`；真实端到端测试再由 frozen Inspection schema 和 `World::apply_stage` 原子提交三个字段。frame-debug capture 绑定严格递增 capture id 与 exact graph signature，在保存前完整验证 compiled node、timing 唯一性、shader diagnostic node、node/diagnostic/总 byte 配额，规范排序 timing/diagnostic，并同时保存 node order、final resource versions 与 alias allocations；失败 capture 不进入 latest-window。定向 4 项与 Voplay workspace runtime 158 tests、protocol 1 test，共 159 passed，warning-free；真实 renderer pick/readback backend、gizmo UI交互、attachment/pipeline bytes 和跨平台 frame-debug differential 仍待后续切片。
- Voplay 新 `PlayModeManager` 以 editor Engine 为 owner 管理有界、generational play session；每个 session 必须使用不同于 editor 且未被其他 preview 占用的独立 Engine/World identity。start 从 exact authoring World snapshot 读取 live stable key/component bytes，在 entity/clone-byte/policy 配额内用标准 Spawn batch 构造隔离 World，并单独保存 stable-key baseline；play Entity 始终保留 play World owner，authoring 与不同 preview 之间没有 Entity handle 共享。exit 显式选择 `Discard` 或 `ApplyAuthoring`；回写要求调用方 expected revision 与 start authoring revision、当前 revision 三者 exact 一致，外部 authoring mutation 返回 conflict 且保持 play session 存活。回写 policy 只允许预先声明的 component byte field，统一验证 identity、width、offset overflow、重复/重叠和数量；退出时按 stable key 同时解析 authoring/play live Entity，只复制相对 baseline 改变的白名单字段，非白名单运行态字段、play-only entity 和 runtime handle 不回写，并以单一 `World::apply_stage` 原子提交。session generation 在任何 authoring mutation 前 preflight；成功退出推进 generation、释放 slot，旧 PlayModeId fail-closed。定向 4 项覆盖双 preview 隔离、白名单回写、外部冲突保留、discard/容量/slot 复用；最新 Voplay workspace runtime 162 tests、protocol 1 test，共 163 passed，warning-free。完整 GameEngine/supervisor 启停、独立 input/render/asset scopes、Studio 多 Session preview 容器与真实 play-mode shutdown smoke 仍待后续切片。
- 冻结依赖方向要求 editor UI 位于独立集成模块，现已新增 `voplay-vogui-editor` crate；Vogui core 保持对 Voplay 零依赖，集成 crate 仅消费两边公开 API。`EditorClient` 为 Hierarchy/Inspector/Assets/Schedule/Render/Performance 六类 panel 注册完整 canonical schema，要求 schema type/version/title/列集合有效且六类齐全后 freeze，并按 panel/column 规范顺序生成稳定 fingerprint；freeze 后禁止 mutation。Voplay typed page ingestion 重验 Engine owner、panel kind、source revision、requested/next cursor 连续性，continuation 的 stale revision/skip/duplicate row 原子失败；每行必须与冻结列集合 exact 匹配，并在 row count/row byte/panel byte 三重配额内提交。Inspector 直接消费 World inspection page，其余五类消费 `EditorPanelPage<T>`；全部转为 stable-key standard Vogui `ViewNode`，测试通过真实 `UiSession::commit_views` 产生 retained-tree transaction。独立 typed request queue 覆盖 refresh、selection、Inspection edit、pause/step/resume、pick/cancel 和 play-mode exit，统一绑定 Engine、expected revision 与有界 encoded bytes；response 同样携带 Engine owner，跨 Engine 回包不消费 pending。endpoint restart 对 dispatched/queued request 分别返回 OutcomeUnknownOnRestart/DroppedBeforeDispatch。Voplay 三 crate workspace 为 runtime 162、protocol 1、editor integration 4，共 167 passed；Vogui runtime 26、protocol 1，共 27 passed，均 warning-free。真实 Studio panel 容器、DOM/retained-GPU renderer widgets、用户事件到 typed request 的端到端路由与平台 smoke 仍待后续切片。
- Studio 的单 guest slot 已由 App Runtime 通用 `SessionHostMap<T>` 替换。该容器直接使用共享 `SessionHandle` 代际身份，设固定容量，支持 transactional factory rollback、精确 get/remove、稳定 live handle 枚举与 slot generation 复用；跨 preview mutation 隔离、容量不扰动 live host、失败建造回滚、单 preview close 不影响 peers、旧 handle fail-closed 共 4 项定向测试通过。Native `AppState` 现在保存最多 16 个独立 `GuestRuntime`，每项拥有 guest thread handle、render buffer 与 BrowserRuntimePlan；`cmd_run_gui` 返回 typed preview handle，sync/async event、island transport、render poll、renderer snapshot 与 stop 全部显式路由该 handle，原 `gui_session_id`、`Mutex<Option<GuestRuntime>>` 和单一 browser runtime plan 已删除。WASM 同样保存最多 16 个 `BrowserSessionHost`，每项独立拥有 guest、RenderBuffer、HostRequest queue 和 browser timer handle 表；timer closure 捕获 exact SessionHandle，关闭会只清理对应 timer/queue/runtime，slot 复用后 late callback 无法进入新 preview。启动完成后若 frontend session 已 supersede，native/browser 都立即按返回 handle 回滚关闭；frontend backend 只保存 App Runtime identity，不导入 framework 私有 runtime 类型。Studio 原生 `find_on_widget_handler_id`、Tauri 对 `vogui-protocol` 的 Git 依赖、frontend ProtocolModule loader、RuntimeService render-byte handler 扫描和 `hostWidgetHandlerId` 状态/API 已整链删除；renderer/host bridge 只按 Resolved runtime 的 `FrameworkContract.jsModules` provider artifact 动态加载，空 provider 走通用无桥接路径。新增纯 `planFrameworkArtifacts` 在加载前按 canonical contract 去重、保持 primary→provider 顺序、按路径去重 host bridge/renderer artifact，并拒绝同一 framework owner 的冲突 runtime contract；定向 site test 覆盖无 artifact、多 provider、共享 bridge、legacy protocol key 不进入加载计划和 owner conflict。最新 App Runtime 全量 93 tests、Studio Tauri 37 tests、Studio WASM wasm32 all-targets check、Svelte/TypeScript check（0 error/0 warning）和 Studio site contract suite 均通过。R9 的 Studio 多 Session/多 preview 容器、独立 shutdown 与 framework-private render scanning 清理已完成首版；renderer/provider instance lifecycle 与真实双 preview 平台 smoke 仍待后续切片，因此尚未形成 ACC/EVID。
- App Runtime 新增 session-owned `DiagnosticsQueue`：每条记录携带 exact SessionHandle/session epoch/CallerEndpoint、单调 sequence、cumulative `dropped_before`、severity、source/code/message；source/code/单条/总字节和 record count 都有硬预算，窗口满时只淘汰最旧记录并显式累计 gap，畸形 owner 或超限记录不改变队列。`SessionKernel::publish_diagnostic` 先通过 EndpointRegistry 的 caller generation/epoch/role 校验；AudioRealtime 仍被拒绝，Closing 明确保留 Diagnostics 发布，finish-close 返回 discarded count 并归零。Hosted AppSession、GuiAppSession、GuestSession、native/web GuestRuntime 共享同一发布/轮询入口。Native event loop 已删除 `on_stdout` 私有旁路，guest stdout 与 VM dispatch error/exit 先进入 Diagnostics endpoint，再由 typed `DiagnosticRecord` callback 转 Studio log；控制型 fatal/exit callback 只保留 lifecycle 信号。WASM 每个 preview 从自己的 guest endpoint 发布 stdout，`pollDiagnostic(preview handle)` 返回 bounded record；WebBackend 为每个 preview 单独验证严格递增 sequence 和 dropped counter 后展示，跨 preview 状态不共享。owner/quota/latest-window、kernel Closing/stale owner、hosted publish/poll/shutdown discard 共 3 层定向覆盖；App Runtime 全量增至 93 tests，alloc-only check、Studio Tauri 37 tests、Studio WASM wasm32 all-targets、Svelte/TypeScript 与 site contracts 全部通过。真实 framework 自定义 structured diagnostics ABI、provider-side category schema、native/browser 双 preview UI smoke 与 fatal race 注入仍待后续认证，因此没有创建 EVID。
- Studio renderer、widget registry、host bridge module 与 host-bridge module graph cache 已改为 preview-owned `ProviderInstanceSet<T>`，全部以 frontend session identity 路由；renderer 启动使用 generation lease，异步 `init()` 被替代或停止后无法迟到安装，失败会按逆序销毁已初始化 renderer/widget 并回收完整 blob graph。单 preview stop 只撤销该 session 的启动租约和实例；全局 stop 同时覆盖尚未安装的 in-flight lease。`PreviewPanel` 的 bridge load/start/render/stop/unload 与 WebBackend 的每次 WASM guest 调用都显式携带 session；HostBridge 的浏览器执行上下文跨 preview 串行，避免异步调用交错覆盖 active owner。`makeVoWebModule` 的同步 VM 导出同样在 exact session context 中运行。新增 generation-aware `ProviderInflightGate` 并接入 renderer render call 与 HostBridge guest call：draining 开始后关闭新 admission，资源 teardown 等待旧 generation ticket 全部 release；replacement 可立即使用新 generation，旧 release 无法影响它。新增独立 provider container/gate contract tests，覆盖双 session 隔离、replacement、expected-instance 迟到 teardown 拒绝、非法 identity、stale/current lease、全量 clear 后 late install 拒绝、pending-only session 枚举、双 in-flight drain、重复 release、drain 中拒绝 admission、旧 generation drain 与 replacement/其他 session 隔离；framework artifact plan 继续覆盖无 provider、多 provider、共享 artifact 和 owner conflict。Vite DEV-only `providerLifecycleSmoke=1` 夹具通过正式 renderer/HostBridge loader API 在真实 Chromium DOM 中创建两个同时可见 surface，并执行 9 项自验证：双实例同时 Active/独立 render、第三实例 init fault、失败实例 destroyWidgets+stop 逆序回滚、peer 存活、单实例 stop 隔离、HostBridge 双 session 缓存隔离、module evaluation fault、同 session 恢复加载、单 session unload 不影响 peer；结果节点为 `data-status=passed`，浏览器 error/warn 日志为空。最新 `svelte-check` 为 0 error/0 warning，完整 site contract suite与 Vite production build 通过；紧邻前一切片的 Studio WASM wasm32 all-targets、App Runtime 93 tests、Tauri 37 tests 源码范围未变化，可继续沿用。该 smoke 为 `ACC-RT-008/013` 和 `ACC-DELIVERY-006` 提供局部实现证据；真实 Vogui/Voplay guest、完整 App Session/Surface/Input/lease 组合、native 平台与最终资源计数仍未覆盖，所以没有创建 EVID，也没有把相关 ACC 标为 verified。
- 对 Studio 多 preview owner 继续向内审计后确认：此前每个 `AppSession::new_hosted` 都自行创建容量 1 的 `AppRuntime`，因此不同 preview 的 ABI caller 可出现相同 session index/generation，只靠不同 `AppHostServicesV2` owner 间接隔离。现已新增显式、可 clone 的 `HostedAppRuntime`，唯一持有共享 `AppHostServicesV2/AppRuntime`，并通过 transactional `create_app_session` 在同一 runtime 中完成 Session allocation、Starting、FrameworkLogic endpoint、Running 和 VM binding；任一阶段失败都会关闭已预留 Session。旧单 Session convenience constructor 已复用该 authority。Native Studio `AppState` 现在创建唯一容量 16 的 HostedAppRuntime，每个 native guest event loop 从其 clone 分配 Session；event loop 在首帧同时返回该 authoritative SessionHandle，`SessionHostMap::bind` 用它绑定平台 host state，前端 native preview handle 因而与 ABI caller 的 session index/generation完全相同，旧 handle 在 runtime/host 双侧复用后都 fail-closed。WASM thread 同样创建唯一容量 16 的 authority，所有 browser GUI guest 经 `new_gui_app_hosted_in` 进入同一 runtime，并在外层 `GUESTS` map 用 `GuestRuntime::host_session_handle` 返回的 exact AppRuntime handle 执行 bind；browser preview handle 现在也与 ABI caller identity 完全相同。仓库无调用者的旧 `startRenderIsland -> GUESTS SessionHostMap` 导出已删除；正式 renderer 使用实例化 `StudioVoVm/RenderIslandRuntime` 对象，其生命周期留在 provider instance，不再伪装为 App Session。受治理 Studio WASM asset 已用固定 build id `rewrite-local` 重建，JS/d.ts/wasm 中均无 `startRenderIsland` 符号。Browser host 在 VM/start/diagnostics/外层 host admission 任一失败时显式 shutdown；外层容量在 guest 构造前预检，`BrowserSessionHost::Drop` 也执行非阻塞幂等收口，避免 host map admission fault 遗留内部 Session。新增组合测试在同一 owner 中同时创建两个 AppSession，证明 caller session identity/epoch 唯一、owner Arc 相同、diagnostics 队列隔离、关闭一个后另一个继续发布/轮询、slot 复用推进 generation+epoch，最后 live session count 归零；新增 authoritative host bind 测试覆盖外部 generation、sparse index、remove/rebind 与 late stale handle。App Runtime 全量为 95 tests；alloc-only check、Tauri 37 tests、Studio WASM wasm32 all-targets、Svelte/TypeScript 0 error/0 warning、site contracts、production build、format 与 diff hygiene 均通过。下一切片仍需为 Session 内 provider/InstanceGroup 建立正式 generational handle/registry，并加入 native/browser 双 preview close 后 runtime live-count 平台观测；相关 ACC 继续保持未验收。
- App Protocol 新增 `InstanceGroupHandle` 与 `ProviderInstanceHandle` 代际身份；App Runtime 新增 Session-owned `ProviderRegistry`，对 template/group/instance 总量、单 group 实例数、required 数量和每 template 并发 group 数执行硬配额，并校验 role、placement、isolation 和 terminal failure scope。Session 在 Created 阶段注册 ProviderTemplate catalog，`SessionKernel::begin_start` 原子 seal catalog；未 seal 时禁止建组，seal 后禁止新增 template，动态实例只能按已批准 template_id 创建。未知 template、重复注册与超额实例化都在 mutation 前拒绝。template 同时冻结 Prepare/Start/Close deadline。Provider 生命周期覆盖 Created→Preparing→Starting→Ready→Suspended/Failed→Closing→Closed，InstanceGroup 覆盖 Created→Starting→Ready→Closing→Closed；required provider 全部 Ready 才推进 group，optional provider 不阻塞 readiness。失败实例 replacement 推进 generation 并拒绝旧 handle；rollback/shutdown 按实例逆序关闭，generation exhaustion、非法迁移、非法 placement/isolation、越权 AppRuntime failure scope 均 fail-closed。`SessionKernel` 封装 catalog 注册、group/instance 创建及全部状态迁移，Closing 后拒绝 ingress，`finish_close` 将逆序 `ProviderShutdownReport` 纳入 SessionCloseReport 并证明 live group/instance 均归零。新增 provider 单元测试 4 项和 kernel owner/shutdown 集成测试 1 项；`vo-app-protocol` 9 tests、`vo-app-runtime` 100 tests、alloc-only check、Studio WASM wasm32 all-targets check、Studio Tauri 37 tests全部通过。当前 registry 已建立正式身份、批准 catalog、配额、状态机、回滚和 Session authority；detached manifest/artifact/factory 校验与实例化、deadline 执行/poison、typed endpoint 绑定、native/browser 双 preview close 后 runtime live-count 平台观测仍待后续切片，因此相关 ACC 继续保持未验收，未创建 EVID。
- ProviderTemplate 的 Prepare/Start/Close deadline 已从静态元数据落实为 Session 级确定性监督：ProviderRegistry 保存单调 observed tick 与每实例绝对 deadline，公开最近截止点供平台调度；time regression、deadline overflow 和截止点到达后的迟到 Start/Ready/Close completion 都在生命周期 mutation 前拒绝。到期只产生一次 typed `ProviderTimeoutEvent`，包含 exact Session-owned instance/group、phase、isolation、failure_scope、deadline/observed tick 和宿主动作。TerminableWorker/ChildProcess 进入 `TerminateInstance`，实例保持 Failed/Closing，只有显式 termination ack 才失效 generation 并释放；CooperativeInProcess 任一调用超时进入 `PoisonAppRuntime`。AppRuntime 新增 `PoisonedRequiresProcessRestart` 终态与首个 poison provenance，poison 后拒绝创建 Session、可变 Session 访问、Session close/reuse，HostServices V2 映射为结构化 internal error；非 cooperative 的 Session-scoped 超时只把所属 Session 置为 Failed，peer Session 与 AppRuntime 保持可用。新增 provider deadline 用例 2 项、AppRuntime poison/failure-scope 用例 2 项；`vo-app-protocol` 9 tests、`vo-app-runtime` 104 tests、alloc-only check、Studio WASM wasm32 all-targets check、Studio Tauri 37 tests通过。deadline 与 poison 合同已具备局部真实测试；平台 actor 的异步 timer 驱动、worker/process terminate adapter、资源 quarantine/诊断、artifact/factory loader 仍待实现，相关 ACC 继续保持未验收且未创建 EVID。
- ProviderInstance 现与 authoritative `CallerEndpointHandle` 一一绑定：未绑定 endpoint 的实例无法进入 Prepare；`SessionKernel::bind_provider_endpoint` 依据冻结的 ProviderRole/placement 映射 BootstrapVm、UiExecutor、EngineLogic、Render、Asset、AudioControl 或 Provider typed endpoint，并由 EndpointRegistry 分配 exact Session/endpoint generation+epoch。restart 在任何 endpoint mutation 前预检 provider state 与 generation exhaustion，随后回收旧 endpoint 所有 request/timer/buffer/wake/audio 资源、解绑旧 caller、推进 ProviderInstance generation；replacement 必须重新绑定新 endpoint，旧 caller 在 EndpointRegistry 与 ProviderRegistry 双侧均 stale。正常 Provider close 先把 endpoint 置为 Closing，业务 HostServices ingress 返回 Closing，deadline 内 close completion 才回收 caller；terminable timeout 只接受显式 termination ack。动态 InstanceGroup rollback 预检全部 generation 后按反向实例顺序关闭 endpoint/resources，再关闭 provider/group；Session shutdown 同样先解绑全部 provider endpoint，再逆序清理 provider，SessionCloseReport 同时保留 endpoint 与 provider 关闭记录。新增跨层 owner/restart/shutdown 扩展断言和 provider close/dynamic rollback 用例 1 项；`vo-app-protocol` 9 tests、`vo-app-runtime` 105 tests、alloc-only check、Studio WASM wasm32 all-targets check、Studio Tauri 37 tests通过。Provider 与 HostServices V2 身份/资源边界已正式相连；真实 factory loader、channel schema negotiation、worker/process adapter 与平台 live-count smoke 继续 pending，相关 ACC 未验收且未创建 EVID。
- Provider factory catalog 已建立三阶段装载权威：`ProviderFactoryRequirement` 在 Resolved template 中冻结 factory id、artifact digest、ABI/schema/capability fingerprint 与 loader kind；映射 artifact 前，`ProviderFactoryManifest` 必须逐字段匹配 role、placement、isolation、static-initializer policy 和 trust evidence，development policy 接受非零 materialization attestation，release policy 只接受内建 factory 或完整 signature-verification/provenance/SBOM digest。catalog seal 会拒绝任何尚未验证 manifest 的 template。artifact 映射后，`LoadedProviderFactory` 再核验实际 factory id、artifact、role、ABI 与 schema；只有 `LoadedValidated` factory 可以创建实例，mismatch 保持 `ManifestVerified` 且不会进入 factory/guest entry。loader/placement/static-initializer matrix覆盖 built-in、native dynamic library、WASM 与 browser JS：同进程 native 要求 initializer proven absent，browser main adapter 要求 certified side-effect-free，worker/process 要求 isolation evidence。每个 factory 维护 live-instance pin；live pin 阻止 unload，built-in/unsafe mapping 明确保留，安全且零实例的 WASM/native/browser factory 可回到 ManifestVerified。Session shutdown 把安全卸载与 retained mapping 分别写入 ProviderShutdownReport。新增 manifest/load mismatch、release trust/pin/unload、shutdown unload/retained 三项 provider 测试；`vo-app-protocol` 9 tests、`vo-app-runtime` 108 tests、alloc-only check、Studio WASM wasm32 all-targets check、Studio Tauri 37 tests通过。当前完成的是 runtime 对 resolver 已验证 trust evidence 的结构/identity enforcement；签名密码学、真实 dlopen/WASM compile/worker import adapter、binary initializer inspection 与 quarantine 仍需在各 loader owner 实现，相关 ACC 未验收且未创建 EVID。
- InitialInstanceGraph 的 runtime authority 已落地：InstanceGroup 明确区分 `InitialRequired` 与 `Dynamic`。Provider catalog seal 后 Session 进入 Starting；只有 Starting 可以创建 InitialRequired group，只有 Running 可以创建 Dynamic attach group，required_count=0 的 initial group直接拒绝。`SessionKernel::mark_running` 先校验生命周期，再要求全部 initial group 的精确 required provider 集合到达 Ready；未满足返回 `InitialProvidersNotReady` 且 Session 仍在 Starting。initial group 内 optional provider fault 保持局部；任一 required provider 在 Running 前显式 fault 或 Prepare/Start deadline timeout 都把 Session 启动事务置为 Failed，随后 Ready/Running transition fail-closed。required provider fault/close/timeout 同时把 group 从 Ready/Created 降回 Starting，防止陈旧 Ready 状态泄漏。Dynamic group 在 Starting 阶段无法创建，InitialRequired group 在 Running 后无法新增。新增 phase/Ready gate、optional/required fault、required deadline timeout 三项 kernel 测试；`vo-app-protocol` 9 tests、`vo-app-runtime` 111 tests、alloc-only check、Studio WASM wasm32 all-targets check、Studio Tauri 37 tests通过。当前 InitialInstanceGraph owner、phase 与 Ready/failure gate 已具备真实内核证据；ResolvedAppRuntimePlan 的批量 graph 安装 API、dependency DAG、optional disable/capability-change policy、真实 AppSession factory 启动事务仍待实现，相关 ACC 未验收且未创建 EVID。
- Provider dependency DAG 与 `ResolvedAppRuntimePlan` 的事务化安装已进入 Session authority：每个 template 使用固定上限 8 的 `ProviderDependencySet`，catalog seal 拒绝零值、重复、自依赖、未知依赖和环，并按声明稳定性生成拓扑序。批量 catalog 先在 staging registry 完成 manifest/trust/load、依赖装载和 DAG 全量校验，任一失败均不改变 live registry；factory load 要求依赖已 `LoadedValidated`，Provider Prepare 要求同 Session 中每项依赖已有 Ready instance。显式 factory unload 会在 loaded dependent 存在时拒绝，group rollback 与 Session shutdown 按反向依赖拓扑关闭实例和 factory。`ResolvedAppRuntimePlan` 冻结 plan identity、generation、effective limits digest、完整 provider catalog 与 initial groups；Session 安装前核对自身 limits fingerprint、初始 group/required provider、endpoint capability 配额及依赖覆盖，随后原子接纳 catalog。`begin_start` 自动物化 InitialRequired group、ProviderInstance 与 exact CallerEndpoint，任一步骤失败都逆序回滚并把启动事务置为 Failed；初始依赖仍由 Prepare/Ready gate 强制顺序。新增稳定拓扑/环与原子重试、factory/instance dependency readiness、plan 批量安装/初始图物化、拒绝计划后 pristine retry 四项测试。最新 `vo-app-protocol` 9 tests、`vo-app-runtime` 115 tests、alloc-only check、Studio WASM wasm32 all-targets check、Studio Tauri 37 tests全部通过；格式化与差异卫生检查随后复核。optional disable/capability-change policy、真实平台 factory actor/loader 与完整 AppSession 启动适配仍待后续切片，相关 ACC 未验收且未创建 EVID。
- Optional provider disable policy 与 capability-change authority 已落地：`ProviderTemplate` 显式冻结 `Forbidden` 或 `DisableCapability`，required template 声明 disable 会在 catalog mutation 前拒绝。optional instance 只有获批 `DisableCapability` 时，failure outcome 才不升级 terminal scope，并发布携带单调 sequence、exact group/instance/template、capability digest 与 `Disabled` state 的 typed change；`Forbidden` 继续返回原 failure scope。change queue 具有独立硬配额，容量或 sequence 耗尽均在 Provider 状态 mutation 前失败；消费后 sequence 继续单调，disable 后拒绝同代 restart。SessionKernel 公开有界 change drain，shutdown 清空未消费项并在 ProviderShutdownReport 报告 discarded count。新增 policy 合法性、局部禁用、队列背压原子性、poll drain、restart 拒绝与 shutdown discard 覆盖；`vo-app-runtime` 全量增至 116 tests，alloc-only check、Studio WASM wasm32 all-targets check、Studio Tauri 37 tests通过。deferred activation/ReadyLocked、restart policy 与 capability re-enable transaction 仍待后续切片，相关 ACC 未验收且未创建 EVID。
- Deferred activation 与显式 restart policy 已进入冻结 ProviderTemplate：`Immediate` template 只接受常规 Ready，`ReadyLockedAllowed` template 可以在 Start deadline 内进入 `ReadyLocked`，该状态证明协议/control/逻辑已可用，满足 dependency 和 required group Ready gate，因此 InitialInstanceGraph 可以推动 Session Running；随后 `activate_provider` 原子迁移到 Ready，并依次发布带 exact owner 与单调 sequence 的 `ReadyLocked`、`Available` capability change。activation 遇到 change queue 背压时保持原状态，Immediate template 的 locked completion 在 mutation 前拒绝。restart policy 只有 `Forbidden` 与有界 `OnFailure { max_restarts }`；零额度以及 disable+restart 冲突在 catalog 接纳前失败。每代 instance 保存累计 restart count，replacement 推进 ProviderInstance generation、endpoint generation/channel owner 由现有 kernel transaction 重新绑定，额度耗尽后拒绝再次 replacement。`ProviderFailureOutcome` 现显式区分 `Restart`、`DisableCapability` 与 `ApplyTerminalScope`，有剩余额度时不提前返回 terminal scope，只有 policy 耗尽或禁止恢复后才应用冻结 failure scope。新增 ReadyLocked→Running→Available、依赖使用 locked Ready、activation 背压原子性、Immediate 拒绝、restart generation/count/额度/禁止策略测试；`vo-app-runtime` 全量为 117 tests，alloc-only、Studio WASM wasm32 all-targets、Studio Tauri 37 tests 与格式检查通过。terminable timeout 后的自动 replacement actor、ReadyLocked 的真实用户手势/设备 adapter 与 capability UI/guest channel 投递仍待平台切片，相关 ACC 未验收且未创建 EVID。
- Resolved plan 已从 SessionKernel 单测入口贯通到 Hosted AppSession 启动事务。`ResolvedAppRuntimePlan::validate` 现在要求 InitialInstanceGraph 中恰好一个 required `session.vm`，缺失、重复或 optional session VM 都在分配 Session 前失败。`HostedAppRuntime::begin_planned_app_session` 在共享 AppRuntime 中依次创建 Session、原子安装 plan、seal catalog、物化初始 group/instance/exact endpoint，并返回 `PendingHostedAppSession`；初始 provider 按 catalog 稳定依赖拓扑暴露给平台 loader/actor。pending 只接受属于该启动事务的 exact ProviderInstanceHandle，提供 Prepare、Start、Ready、ReadyLocked、Activate、状态与 capability-change drain；依赖乱序 completion 由内核拒绝且不改变 child 状态。`finalize` 必须通过全部 initial required group Ready gate，随后才把唯一 session.vm CallerEndpoint 安装进 VM并产生可运行 AppSession；提前 finalize、plan/install fault 或 pending drop 都关闭 Session，走统一 endpoint/provider 反向 shutdown。成功测试确认 VM caller 等于 session.vm endpoint、SessionHandle 与共享 AppRuntime authority 一致，显式 shutdown 后 live Session 归零；失败、放弃和 invalid plan 同样保持 live count 为零。新增 planned success/fault/drop 与 session.vm uniqueness 两项 AppSession 测试；`vo-app-runtime` 全量增至 119 tests，alloc-only、Studio WASM wasm32 all-targets 和 Studio Tauri 37 tests通过。当前平台 actor 仍需把真实 factory instantiate/Prepare/Start completion 接入 pending API，Studio native/browser 仍使用过渡 convenience startup，因此尚未形成完整平台 ACC/EVID。
- App Protocol 新增 allocation-free、zero-copy 的 length-delimited optional section codec，直接使用 schema 生成的 `MAX_OPTIONAL_SECTIONS` 与 `MAX_PAYLOAD_BYTES` 上限。每项采用 nonzero kind、u32 little-endian length 和 borrowed payload；reader 在暴露 payload 前校验总 payload、header、checked length、section count 与 exact slice 边界，unknown kind 由调用方稳定跳过且不重排已知项。新增测试遍历单 section 的每个截断前缀，覆盖 forged u32 length、zero kind、第 65 项拒绝、拒绝前 count/offset 不推进，以及 known→unknown→known 的零拷贝顺序。Channel negotiation 新增真实 N↔N-1 matrix：N 同时声明 minor 1 与 minor 0，N-1 只声明 minor 0 时选择双方共享的最高 exact minor；同 minor fingerprint 不同则 `NoExactMinorMatch`。`vo-app-protocol` 全量从 9 增至 13 tests；依赖它的 App Runtime 119 tests、Studio WASM wasm32 all-targets 与 Studio Tauri 37 tests全部通过。该切片覆盖 TEST-PROTO-001 的 unknown optional、truncation/length/count 和 N↔N-1 exact negotiation 子项；全 decoder fuzz、跨语言 optional-section golden、illegal UTF-8/depth/allocation overflow 与 transaction codec 仍缺真实证据，因此未创建 EVID，未标记 TEST-PROTO-001 完成。
- Optional-section wire layout 已收归 schema compiler 单一权威：`app.schema.toml` 冻结 kind=16 bits、length=32 bits，compiler 验证 major-1 只能使用该组合，canonical major compatibility fingerprint 也纳入两个位宽。Rust、TypeScript 与 Vo renderer 统一生成 `OPTIONAL_SECTION_KIND_BITS`、`OPTIONAL_SECTION_LENGTH_BITS`、`OPTIONAL_SECTION_HEADER_BYTES=6`、section/count limits 和完全相同的 38-byte optional golden literal；受治理生成器新增 `golden-optional-sections.bin` 并将其列入 provenance output digest。Rust codec 已删除手写 header 常量，直接消费 build-time generated contract，并通过 `include_bytes` 对新 golden 做逐字节相等和三段 decode 断言。schema compiler 3 tests覆盖三语言 literal equality、确定性、非法 8-bit kind/64-bit length 拒绝和生成常量；App Protocol 增至 14 tests。`vo-dev generate app-protocol --write` 与随后 `--check`、生成 TypeScript no-emit、生成 Vo package check、App Runtime 119 tests、Studio WASM wasm32 all-targets、Studio Tauri 37 tests全部通过。跨语言生成 literal 与编译证据已经取得；TypeScript/Vo 独立 encoder 运行后与 golden byte equality、全 decoder fuzz 仍待后续，因此 TEST-PROTO-001 继续保持未完成且未创建 EVID。
- TypeScript 与 Vo 的 optional-section encoder 已由 schema compiler 正式生成并取得独立执行证据。TypeScript `encodeOptionalSection` 使用 Uint8Array/DataView little-endian 写入 kind/length，`decodeOptionalSections` 对 total bytes、count、header、kind、safe payload end 和 truncation 执行有界校验，payload 通过 subarray 保持 zero-copy；Studio 新 `test:app-protocol` 从磁盘读取受治理 `.bin`，独立编码三段 known/unknown/known 后逐字节同时匹配 binary golden 与生成 literal，并验证 decode 顺序、forged u32 length 和第 65 项拒绝。Vo renderer 生成同合同的 `EncodeOptionalSection`，另生成 standalone `golden-optional-sections.vo`；`target/debug/vo run` 实际编码三段 payload，逐字节比较 compiler expected literal，输出成功标记。两者均未复用 Rust encoder。TypeScript no-emit、Vo package check、Vo standalone runtime、generator `--check` 和完整 Studio site contract suite通过；Rust App Protocol 14 tests、App Runtime 119 tests、Studio WASM wasm32 all-targets、Studio Tauri 37 tests再次通过。TEST-PROTO-001 的 App optional-section Rust↔TypeScript↔Vo golden byte equality 子项现有真实证据；framework transaction codec、全 decoder fuzz、UTF-8/depth/allocation overflow 等其余子项继续 pending，因此暂不建立整项 EVID。
- App Protocol 新增独立 `fuzz/app-protocol` libFuzzer workspace 与 `decoders` target，覆盖 raw input 及 governed envelope/optional golden、有效 ChannelOpen seed 的 mutation，并逐次调用 envelope、ChannelOpen、ChannelAccept、optional-section 全部现有 decoder；成功 decode 路径强制执行 payload 边界与 encode→decode exact round-trip，optional sections 还校验 consumed/count 上限及重编码逐字节相等。Apple ARM64 本机以稳定工具链显式启用 sancov module、level 3 与 inline 8-bit counters，加载 441 个计数器；固定 seed 424242、`max_len=4096` 的 100,000 次 campaign 将 feature coverage 从 61 扩展到 191、语料增长到 64 个/3880 bytes，零 crash、零 panic。可复现构建/运行命令已记录在 fuzz README。当前工具链没有 ASan runtime，运行日志明确报告三个 sanitizer callback 缺失，因此该结果只登记为真实 coverage-guided fuzz 子项；address-sanitized memory-corruption campaign、framework transaction decoder、UTF-8/depth/allocation overflow 仍 pending，TEST-PROTO-001 继续不创建整项 EVID。
- 中央 `fuzz/framework-protocol` workspace 通过只读 path dependency 实际链接 Vogui/Voplay owner crate，`framework_decoders` 覆盖两套 packet decoder、Vogui `RendererMirror` 的 bounded transaction decoder 以及 Voplay partition chunk decoder。目标同时 mutation 有效 Vogui session/root packet、Voplay engine packet、可创建完整 root 的 Vogui snapshot transaction 和可重编码空 partition chunk；packet 成功路径核对 exact payload/packet length，Vogui 失败路径核对 last-good revision 不变，partition 成功路径要求 encode→decode exact equality。Apple ARM64 sancov 构建加载 41,708 个 inline 8-bit counters；固定 seed 424242、`max_len=4096` 的 100,000 次 campaign 将 feature coverage 从 90 扩展到 996、语料增长到 215 个/10,983 bytes，零 crash、零 panic。隔离 workspace offline check 与可复现 README 命令通过。该 campaign 同样缺少 ASan runtime；Audio control snapshot 等结构化 decoder、未来公开的 RenderState transaction wire decoder 和恶意 UTF-8/depth/allocation fixture 仍需独立覆盖，因此“fuzz 所有 decoder”尚未整体验收。
- 中央 `vo-dev` 生成器统一支持 app/vogui/voplay 三个协议，三套 `--check` 均通过；Voplay TypeScript no-emit 通过。Voplay Vo check 进入项目检查后长期无输出并由实施代理中止，不能记为通过或 EVID。
- `vo-dev lint artifacts`：当前仅因新生成目录尚未进入 Git tracked set 失败。commit/stage 未获本任务要求，保持治理策略和索引原状；生成器内容检查已经通过。
- Voplay 实际游戏呈现主链继续完成编码：`GameEntryDescriptor` 现精确绑定 factory/artifact/schema/init/provider/role，factory 只消费有界 owned init bytes；`GameEngine` 支持 Constructed→Running↔Paused→Closed 生命周期。新增 Engine-owned `DeviceHub`、`SurfaceRuntime`、`PresentationRuntime`、portable draw command encoder 与可实际光栅 Clear/SolidRect 的 RGBA headless backend；Surface pulse 经过 endpoint/device generation、RenderRevision、RenderControlRevision、frame/pulse、deadline 与 ACK 屏障后执行 Present。
- Logic `GameContext` 已按 stage 收口：Simulation World 对 system 只读，tick/startup/shutdown 的结构变化进入有界 World CommandBuffer，并在整个 stage 成功后原子提交；Extract/Frame 的 World mutation 在运行时拒绝。Extract 产出 durable `RenderOp`，Frame 产出 per-domain transient 与独立 `PresentationState` transaction；RenderOutbox 批量 transient 校验、PresentationState 与 transient 提交均避免半批状态。PresentationState 按 domain/frame 有界保存展示状态，tick 无权访问，且不进入 World/Clock deterministic hash 或 recovery capsule。
- `PresentationRuntime` 已直接消费 RenderOutbox durable transaction、回送 exact ACK 并按 frame 取得 ready transient，形成 GameEngine→RenderOutbox→RenderWorld→encoder→backend→Surface Present 闭环。新增 `runtime::run` 与 `VoplayRuntime::{attach,pulse,manual_step,pause,resume,detach,shutdown}` façade，把 DeviceHub lease、Surface 集合、backend/encoder 与 GameEngine 统一持有；设备 binding 变化会停止旧 generation 提交。`app-session` feature 通过 Hosted AppSession 动态 InstanceGroup 适配 `begin_attach/attach`，动态 provider/endpoint 组安装为单次事务，失败、pending abandon 与 hosted group drop 都走统一 rollback。
- App Runtime 新增 `DynamicInstanceGroupPlan`、`SessionKernel::install_dynamic_instance_group`、`PendingHostedInstanceGroup` 与 `HostedInstanceGroup`。动态 attach 只允许 Running Session，要求至少一个 required provider、拒绝重复 template，按冻结 catalog 创建 provider 和 exact endpoint；整组达到 Ready 后才 finalize。Voplay `app-session` feature 必要编译通过。GameEngine recovery capsule 绑定 Engine、完整入口描述、schedule hash、World snapshot 与 SimulationClock snapshot，恢复只允许空的 Constructed Engine 且先完成身份验证；PresentationState 明确排除。上述阶段仅执行必要格式与编译检查，未提前运行高级测试或创建 ACC/EVID。
- Voplay SimulationWorld 状态域继续补齐：新增有界 `SimulationState`，统一承载 World-owned Resource 与 stage 间双缓冲 Event。GameContext 提供只读 resource/event view，resource mutation、event emission 与 Entity/component structural command 一样先进入 stage-local bounded buffer；presentation stage 无权写入。stage 全部 system 成功后先完成 Resource/Event 与 World command 预检，再提交 World 和 SimulationState；失败不留下半批状态。Engine 维护独立单调 `SimulationRevision`，World、Resource 或 Event 任一提交都会推进；SimulationClock hash 同时混入 WorldRevision、SimulationRevision 和 canonical SimulationState digest。recovery capsule 当前 schema 3 保存 SimulationState、SimulationRevision、尚未进入 safe point 的 endpoint completion 和 LogicIo 命令/sequence 水位，并核对 revision、容量与身份。
- Voplay renderer/device recovery 已进入公开 runtime façade：RenderOutbox 可从 acked、inflight、pending 或 retry 中的 snapshot 构造有界 compact projection，切换 channel epoch 后产生单一 recovery snapshot；PresentationRuntime 同步重启 RenderWorld、应用 snapshot 并 exact ACK。`VoplayRuntime::recover_renderer` 同时覆盖局部 `RendererRecoveryRequired` 与 Hub `AwaitingDeviceRebind`，重新绑定 backend framebuffer、Surface endpoint generation 和 device generation；全部 Surface 成功后才调用 DeviceHub completion 并恢复 Ready。重复 recovery 能从 snapshot-inflight/acked projection 再切新 epoch，已完成的 Surface rebind 按 exact binding 幂等跳过。该批仍只执行必要格式及 `voplay-runtime --features app-session --offline --locked` 编译检查。
- Voplay role endpoint ABI 已开始落到可执行实例：protocol message catalog 从原 9 类扩展为 lifecycle、tick/frame、RenderState/Control/Transient/Event、Audio、Asset、Physics、Surface/Device、Diagnostics、Inspection 与 WorkerWake 共 32 类；packet 上限与独立 snapshot object 上限进入 schema fingerprint，Rust/TypeScript/Vo/provenance 已由受治理生成器同步。`voplay-protocol::encode_packet` 与 runtime `EndpointArena` 提供 Engine/role/actor/artifact/schema exact binding、generational instance handle、Create/Dispatch/SignalWake/PollAfterWake/Close/Destroy、poll count/byte budget、manual deterministic pump 与 inbound/outbound kind matrix。
- Render wire 新增 canonical bounded codec，覆盖 RenderStateTransaction、RenderStateSnapshot、RenderStateAck 和 per-domain RenderTransient；decoder 在 allocation 前验证 packet/count/minimum bytes、owner、revision/header、entity generation、duplicate object、payload exact EOF 与 transient byte budget。具体 `RenderEndpointDriver` 处理 Start/Suspend/Resume/Close、原子 transaction/snapshot apply 与 exact ACK；具体泛型 `LogicEndpointDriver<Game>` 处理 Start、TickInput→SimulationTick/TickResult、FramePulse→Extract/Frame→RenderState/Transient、RenderStateAck、Pause/Resume/Close，并复用同一 GameEngine。两端经统一 EndpointDriver 接入 EndpointArena，最新 `voplay-runtime --features app-session --offline --locked` 必要编译 warning-free；尚未提前运行 endpoint 集成/畸形 wire 高级测试或登记 EVID。
- Voplay Asset/Audio role endpoint 主链已进入统一 EndpointArena。`AssetEndpointDriver` 直接持有 AssetServer，处理 Start/Suspend/Resume/Close、注册批次、hot reload、AssetScope 创建/关闭/释放、ticket request/cancel/expire/release、异步 AssetWork 派发、带 endpoint generation 的 completion 与 restart 重派；registration/dependency/output count 和 byte budget 均在分配或 mutation 前校验。`AudioEndpointDriver` 直接持有 AudioEndpoint 与 AudioMixerEndpoint，canonical AudioControl snapshot wire 在 allocation 前校验 entry/descriptor/owner/revision/exact EOF，提交后同时推进 realized mixer 与 one-shot barrier；ReadyLocked、gesture activate、pause/resume、clock、device lost/restart、executor terminal outcome 和 control/event ACK 均有 typed packet。该切片只执行 `cargo check -p voplay-runtime --features app-session --offline --locked` 与 `cargo fmt --all -- --check`，两项通过且 warning-free；完整 endpoint integration 与异常 packet 测试继续留在全部编码完成之后。
- Render/Audio control snapshot 已统一到同一 canonical control wire，按 domain 使用独立 magic 和 message kind，同时复用 entry、tombstone fence、descriptor budget、duplicate identity 与 exact EOF 合同。Render control 新增正式 decoder，将 desired RenderTarget/RenderView descriptor 解成 endpoint-owned realized state，校验 Engine/domain/revision、target/view 配额、stable target dependency、descriptor canonical round-trip 和完整引用；`RenderEndpointDriver` 现接收 RenderControlTransaction、原子替换 realized control state、推进 RenderControlRevision 并返回 exact RenderControlAck，后续 RenderState barrier 直接消费该 revision。必要编译与格式检查通过。
- RenderEndpoint 的 one-shot event 主链也已接通：RenderEvent 按 RenderRevision/RenderControlRevision/deadline 三重 barrier staging，经注入的 renderer event executor 执行并产生 typed RenderEventResult；默认 executor 明确返回 Failed，避免把未执行操作误报成功。state/control commit、WorkerWake 和单调 renderer clock 都会继续 drain ready event；renderer restart 对未 dispatch 与已 dispatch 事件沿用既有确定性终态合成，并推进 channel epoch。最新必要格式与 feature 编译检查继续 warning-free。
- RenderEndpoint 现可选持有完整 presentation adapter：SurfaceControl 支持 attach/resize/suspend/resume/close/rebind 与 endpoint/device generation replacement；RenderTransient 按 domain 只保留递增 frame 的最新有界值；48-byte exact FramePulse 同时绑定 pulse id、now/deadline、Surface/Domain 和 metrics。呈现前从 realized RenderView 集合生成稳定 graph signature，执行 encoder，再经过 SurfaceRuntime 的 exact pulse、RenderRevision/ControlRevision barrier、backend render ACK 与 Present。Engine 全局暂停只恢复由该次全局暂停改变的 Surface，保留独立 SurfaceControl 暂停；malformed pulse、旧 transient 与错误 binding 在推进 pulse 前拒绝。
- GameContext 新增 stage-local `LogicIo`：Startup/PostTick 可提交 Asset control 与经 EngineControlStore lease 构建的 Render/Audio control transaction；simulation stage 可请求 Asset、发出 AudioEvent，Extract/Frame 可发出 RenderEvent。所有 World/Simulation/Presentation/Control/LogicIo 输出先完成批量预检，系统失败不会留下前序 role 命令或 control revision。control 在 cloned store 上原子提交，safe point 产生 typed promotion SimulationEvent，并自动发送完整 desired snapshot。
- LogicEndpoint 已把 Game LogicIo 编成 AssetRequest/AssetControl、Render/AudioControlTransaction 和 Render/AudioEvent；输出 packet/byte budget 先整批 admission，成功后才消费 outbox。AssetCompletion、AudioEventResult、RenderEventResult 经结构校验后进入 bounded pending SimulationEvent，并在下一 tick safe point 发布；Render/Audio ControlAck 将对应 StableControlRef 标成当前 endpoint generation 的 Live。启动状态新增 `Starting` gate，Startup control snapshot 和 Asset control completion 全部返回后才输出 EngineReady，Tick/Frame 在此之前不会执行。上述变更继续只跑必要格式与 feature 编译检查，均 warning-free。
- Vogui `UiResourceStore` 已补入 UiSession owner：Image/Font/IconSvg/Cursor/LocalizedText/CanvasPaint 使用 session-qualified ResourceId 与 root-qualified generational lease；最后 lease 释放会取消 pending fetch，root detach 原子预检并释放该 root 全部 lease。fetch work 同时绑定 ResourceId generation、source revision、job generation、deadline、descriptor 和 content hash，completion 在 mutation 前校验全部身份、digest、单项/总 bytes 与 metadata budget；hot reload 保持 ResourceId、推进 revision/job generation，并使旧 completion O(1) 失效。
- 每 renderer 独立的 `UiRendererResourceCache` 使用 ResourceId + source revision + renderer generation + optional DeviceGeneration 建立 decode/residency work；late renderer/device/source/job completion 拒绝，handle bytes 有独立单项/总预算，同一 source 可进入多个 cache。renderer restart 清除本地平台 handle并允许同 DeviceGeneration 重建，UiResourceStore 的逻辑 source 保持。UiSession 已公开 register/acquire/release/fetch/complete/hot-reload/publication façade；Vogui workspace 必要格式与 offline locked compile 通过且 warning-free。
- Vogui `WidgetProviderRuntime` 已实现 kind/schema/catalog 握手、local/remote placement、session/root-qualified generational WidgetInstanceHandle、Create/ApplyProps revision 与 change bitmap、bounded async Measure、revisioned SemanticsSnapshot、Dispose 即时 generation 失效及 provider fault policy。ProviderEvent 校验 UiRootEpoch、AppCodeEpoch、renderer generation、props revision、provider sequence 与声明事件 kind后，由当前 widget binding 映射 live EventToken，包装为标准 UiReturn Event；provider API 不接触 MapperId。renderer restart 清除本地 cache/sequence 并保留逻辑实例。
- Vogui renderer-owned 输入状态机现覆盖 UTF-8 byte offset/边界校验、Text/EditSequence/Selection/ReplacementRange、受控 model ACK、IME start/update/commit/cancel 与 composition 期间 model patch 延后。pointer capture 优先于 hit path，focus scope、focused node 和 physical key pressed set 均有硬配额；root close 或 renderer suspend 会为 capture、pressed key 和 active composition 合成 cancel/release，并清理对应 owner 状态。
- Vogui Layout/Style/Theme 合同已采用静态类型：Block/Flex/Grid/Stack/Absolute、Auto/Px/Percent/Fraction/MinContent/MaxContent/FitContent、typed edge/overflow/align/justify/aspect ratio，以及 typed color/text/cursor/visibility 字段。UiSession-owned `StyleStore` 按完整内容 intern 出 session-qualified StyleId，严格按 renderer default→theme token→control variant→reusable style→inline→interaction state 解析；theme revision、light/dark、高对比度、reduced motion、density、typography scale、locale/RTL 与 per-root override 均已进入 owner，root detach 会清理 override。
- Vogui Router 与 NavigationLease 已建立真实 owner 状态机：每 Window 唯一 primary URL owner、memory/namespace 隔离、显式 offer/accept transfer ACK 与 lease generation；primary close不产生隐式接管。Route table 覆盖 typed stable RouteId、literal/parameter/rest、nested layout chain、redirect depth/cycle、not-found/error/lazy capability，location/query/hash 有硬预算。Router 由 UiSession 可选持有，支持 navigation id、back/forward、独立 root RouteScopeId、root detach 清理绑定；Vogui workspace必要 offline locked compile保持 warning-free。
- Vogui 两阶段热重载的 owner 协调器已完成编码：preflight 会返回首个不可转移 active Effect blocker；blocker 清零后固定 old AppCodeEpoch、sequence barrier、deadline 与 hold count/byte budget。barrier 后 Message/Event/EffectCompletion/CommandResult 只能按旧 epoch 与严格递增 sequence 进入 hold queue；candidate 使用中性 snapshot、完整 DriverSchema 与 root/subscription/widget/resource 汇总，超预算或 deadline 到期可在 commit 前原子 abort 并按原序归还 hold item。唯一 commit point 单调分配新 AppCodeEpoch并保持 transferable Effect 清单，replacement snapshot ACK 后才释放 held owner；UiSession/AppDriver candidate 构建与旧 module pin/drop glue 的最终接线仍在后续编码项中。
- Vogui portable control 与 accessibility 合同已落到同一 NodeId 身份：完整 ControlKind catalog 为每个 core control 固定 SemanticRole、keyboard policy、accessible-name、focus scope 与 renderer measurement ownership。`SemanticTree` 对 root/tree/semantic revision、node/action/relation/string预算、唯一可达树、relation target 与交互控件 accessible name做 staging 校验后唯一替换；focus order稳定，平台 accessibility action 绑定当前 revision、NodeId、声明 action 与严格 sequence进入有界队列。
- renderer presentation clock 新增 generational AnimationId，覆盖 transition、keyframe、spring、enter/exit 可复用属性、delay/easing、completion opt-in 与 reduced-motion。动画按单调时钟采样，不产生逐帧 App Message；同 node/property retarget、node删除、root关闭和 renderer restart产生确定性 terminal，generation 与 completion容量在 mutation 前预检。新的 `RendererActor` 统一拥有 RendererMirror、输入/IME、SemanticTree、per-renderer resource residency 与 AnimationRuntime；semantic snapshot必须精确匹配 applied tree revision，动画只能绑定 mirror 中 live NodeId，renderer/device generation restart统一清除平台态并返回合成输入和动画 terminal。
- 两阶段 reload 现已接入真实 UiSession/Async owner。Effect 默认不可跨 reload，只有显式 `begin_transferable_effect` 才进入 preflight 可 hold 集合；barrier 生效后禁止创建新 Effect、普通 completion drain 和普通 UiReturn ingress，Effect completion 与 ApplyAck/Event/CommandResult 必须经带 now/sequence 的 reload ingress进入 typed HeldPayload。hold count/byte/deadline/epoch/sequence 在完成 Effect mutation前预检。candidate commit先 preflight Subscription generation，再原子推进 AppCodeEpoch、停止旧 Subscription、终结旧 Effect/Command、清空旧 return lane，并为每个 root安排 replacement snapshot；旧 epoch transferable Effect ID 作为 retired cleanup 清单保留。emergency reload 在事务 reload 活跃时拒绝，snapshot ACK 或 commit 前 abort分别返回有序 held item供新 epoch terminal/旧 epoch replay处理。AppDriver ModelSlot/candidate build/module pin/drop glue仍是下一接线切片。
- Vogui `AppDriver` 固定 ABI 已完成首版编码：AppEntryDescriptor/OwnedInitData 只携带 artifact/factory/schema identity 与 owned bounded bytes；ModelSlot、MessageSlot 在 owner executor内使用 fingerprint+owned bytes，Channel/renderer API不暴露 `any`。Schema 固定 app build、model/message/transitive ABI fingerprint、transaction mode 与 snapshot/migration capability；driver入口覆盖 Init/Update/Build/BuildSubscriptions/DispatchMapper/neutral snapshot/migrate/OnError。`AppOwner` 校验 factory与schema exact match、model/message/effect/subscription预算，显式规范化 DirtyAllRoots/DirtyRoot/DirtyScope+ScopeBuilderId，并从 candidate model构建受影响 root/scope。
- `UiSession::commit_app_transaction` 已把 candidate model、全部受影响 RetainedTree、subscription diff 与 staged Effect 组成同一预检后提交路径；Effect批次和 Subscription replacement在 tree mutation前完成 quota/generation/ID exhaustion预检，随后才推进 model revision和启动 staged owner。App reload candidate在旁路 new driver中解码中性 snapshot、构建全部 root与完整 Subscription set；`commit_app_reload` 要求 candidate root集合精确覆盖 live root，在单一 owner turn内替换 tree、推进 AppCodeEpoch、清理旧 async owner、安装新 Subscription、切换 driver/model并强制各 renderer generation取得 replacement snapshot。旧 driver Box 仅在该 commit 返回后释放；显式 module pin provider与生成 typed adapter尚待 Volang构建入口接线。
- Vogui profile resolver 已将 host=headless/browser/native-webview/native-gpu、renderer=fake/dom/retained-gpu、controls=minimal/full/editor、composition=standalone/overlay 与 dom-unsafe extra规范化到冻结的13个发布别名。非法 host/renderer、headless overlay、full/editor overlay和非 DOM dom-unsafe在 resolver阶段失败；capability set显式派生 App/reconciler/semantics/resource/input/IME/layout/controls/animation/accessibility/inspection/editor/transparent surface。artifact audit要求每个别名的必需 component，并对所有 profile拒绝 Audio/Voplay renderer/Physics；minimal拒绝 advanced/editor/inspection，DOM/native/headless互斥 renderer与accessibility adapter。
- Vogui revisioned Inspection endpoint 已完成：只读 snapshot复制 UiRoot/Scope/Node、semantic tree、computed layout、StyleId/theme token、focus chain、event/ref/resource/subscription owner、dirty reason、timing/allocation、renderer health/capability与accessibility diagnostic；publish先验证 session/root/revision、全树可达、parent关系及 node/binding/string硬预算再唯一替换。query必须携带 exact inspection revision；编辑使用独立 typed AppMessage或development StyleOverride endpoint，校验 expected inspection/tree revision、live NodeId、sequence与payload预算，Inspection从不持有 model或平台对象引用。
- RendererMirror 已拆成 prepare→platform apply→commit：bounded decoder与轻量 mirror先生成完整验证的 `PreparedRendererApply`、稳定 mutation list和 immutable desired tree snapshot；平台成功后才推进 applied revision。`PlatformRendererActor` 在平台异常时冻结输入、隐藏或detach受损 root并进入 Poisoned，后续只允许 replacement/restart路径。HeadlessRenderer实际逐 mutation执行并与 desired snapshot逐字段核对；retained GPU renderer分离 layout/paint dirty set、向最近 formatting root传播 layout失效，维护 font/style/width/locale/direction text cache key、front-to-back hit-test index、stable NodeId accessibility revision、paint list与device-generation UiSurface，device loss会清理 painter cache并等待rebind。
- clean-slate TypeScript `DomRenderer` 已建立，无 Preact/通用 VDOM/第三方控件依赖。每个 UiRoot拥有 ShadowRoot默认或显式 light-DOM mount、NodeId platform table、typed property applier、root delegated event、NodeRef table、observer/resource/portal/AbortController owner。batch先在纯 mirror校验完整 identity、epoch/revision、node/child/property/string预算与可达树；replacement在 detached fragment构造后swap，普通 patch直接顺序执行。DOM API异常不会推进 revision，会 abort listener、冻结pointer、隐藏并标记 poisoned root请求replacement。关闭统一释放 observer/resource/portal/ref/node。复用 Volang Studio已安装 TypeScript compiler执行 `tsc --noEmit` 通过，未安装新依赖。
- Vogui NodeRef 已从 NodeId包装升级为独立 `LogicalNodeRef` registry：logical handle绑定 UiSession/UiRoot与自身 slot generation，状态完整覆盖 CreatedUnbound、Bound、Unbound、PendingRebind、Closed；NodeId和binding generation属于当前绑定。rebind/unbind、release、root close均在 generation mutation前预检；renderer restart和AppCode reload只把 live binding转为PendingRebind，replacement snapshot ApplyAck后恢复Bound。RetainedTree subtree删除会先枚举旧 subtree全部NodeId并预检关联ref generation，tree commit后同一turn统一Unbound，stale command/result以logical handle+NodeId+expected binding generation O(1)拒绝。
- UiCommand现携带logical NodeRef、target NodeId、expected binding generation、UiRoot/AppCodeEpoch、`min_applied_revision`与deadline。UiSession在创建command时从live binding和当前root tree revision冻结barrier，result同时验证renderer observed revision与当前binding。renderer-owned `RendererCommandRuntime`对root/epoch/app epoch/sequence/future-revision-window/deadline和staging quota执行验证；早于patch到达的command有界等待，revision达到后重新校验binding再Ready，node删除/rebind、deadline、过远future revision产生明确terminal。renderer restart归还所有staged terminal并清空平台ref table；RendererActor applied revision推进会同步drain ready command。
- Vogui `InteractionRuntime` 已纳入 UiSession owner：MIME offer 只承载有界 inline bytes 或 owner-qualified generational `ExternalFileHandle`；clipboard 与 chunked file read 均使用有界异步 request/completion、deadline、offset/size/eof 和 exact handle 校验；drag session 以 generational handle 和严格 event sequence 管理 enter/over/leave/drop/cancel；form registry 保存 revisioned field value/edit sequence、required/validation/help/error并产生有序 submit。root detach 会先完成 refs/interaction generation 与event容量预检，再取消 drag、clipboard、form，避免半清理。
- DOM renderer 的 NodeRef table 已保存 logical ref→NodeId/binding generation/platform node 三元绑定；replacement 使用独立 detached ref table并在 swap 后唯一提交，旧平台引用不会穿越 snapshot。DOM `UiCommand` owner 校验完整 renderer identity、严格 sequence、deadline、future revision window和pending count；barrier 未达到时有界暂存，revision推进后重验 live binding并执行 focus/blur/scroll/measure/selection/composition cancel，poison/close为全部pending command返回terminal。受控输入记录本地 edit sequence与composition状态，旧value ACK或IME期间value patch不会覆盖本地输入；UTF-8/UTF-16 offset转换拒绝拆分code point/surrogate。
- clean-slate Web interaction bridge 已完成浏览器 clipboard读写、DataTransfer MIME/file offer、generational external File table、异步chunk read/release、request/deadline/capacity与统一close；native-WebView侧提供带bridge epoch/request identity和pending配额的纯异步 typed transport client。最新 Vogui Rust workspace `cargo check --workspace --all-targets --offline --locked` 与 JS strict `tsc --noEmit` 均通过；仍只执行编码阶段的编译/静态检查。
- DOM delegated input 已改为 renderer-owned EventToken binding table：patch 显式 bind/unbind `(NodeId,event type)→generational token` 与 once/passive/prevent-default/stop-propagation policy，snapshot replacement独立构造event table，节点删除后残留binding在commit前被拒绝。返回事件只携带root/renderer/revision/NodeId/EventToken/sequence与Press、TextEdit、Key、Focus、Pointer、Drag静态payload，不再把浏览器原始Event传入应用。TextEdit包含本地edit sequence、UTF-8 selection和composition状态。ApplyAck、Event、UiCommandResult现使用同一`DomReturn`类型与sink，ApplyAck在revision发布后先入lane；新增有ACK保留槽、byte/item预算、相邻continuous coalesce和可靠溢出冻结回调的`DomReturnLane`。JS strict `tsc --noEmit`继续通过。
- `DirtyScope` 已从表面 dispatch 接通为真实局部提交：RetainedTree 为每个节点计算稳定 keyed-path scope hash，并以 `(path hash, NodeId generation)` O(1语义校验后定位 live subtree；UiSession 可从 live NodeId生成 `ScopeId`。`AppPreparedTransaction` 现在保留每个 `BuildRequest`，提交阶段按 root 聚合 scope builder结果，单 scope直接 subtree reconcile，同 root多 scope在临时tree上顺序合成后以一个UiTransaction/commit revision原子发布，Root dirty仍执行完整reconcile。scope replacement只复制root外 retained records，patch/count成本随目标subtree变化；scope消失、generation变化、hash碰撞和root/scope混用明确失败。节点删除patch已统一改为children-before-parent后序，修复旧实现按handle顺序删除时平台mirror可能先删非空父节点的问题。Rust workspace all-targets check通过且无warning。
- Subscription 已升级为 App/UiRoot/Scope owner-qualified identity，stable key仅在owner内唯一；registry的handle保持与替换、generation exhaustion、count/byte preflight均基于 `(owner,key)`。generated App adapter接口现返回 `Unchanged | DirtyOwners | ReplaceAll`，局部请求只调用对应App/UiRoot/Scope builder并验证返回spec归属，多个owner diff保留未声明owner的live handle。App提交在真正修改tree前合并scope删除产生的空owner集合与应用声明并一次preflight，随后同一serial turn提交tree与subscription diff；删除scope即使应用未请求重建subscription也会直接Stop该owner完整集合。detach root同样先预检并归还UiRoot及其全部Scope subscription stop changes。Rust all-targets check继续通过且无warning。
- Browser `DomHost` 已成为显式平台owner：按generational UiRoot唯一持有DomRenderer、统一UiReturnLane和BrowserInteractionBridge，集中执行attach/apply/command/poll/drain/detach/close；可靠return overflow会立即冻结pointer并标记root，detach先让renderer为pending command写terminal，再drain lane并清理interaction/DOM host状态。Rust侧新增`PlatformAccessibilityAdapter`与`AccessibilityActor`，semantic snapshot先在clone tree完整验证，再调用平台adapter，平台apply失败会冻结action、detach/hide tree并poison，成功才提交semantic revision；平台action仍经当前tree/semantic revision和NodeId/action集合进入同一有界队列。JS strict tsc与Rust all-targets check通过。
- Effect route table 已进入 generated App descriptor：每个kind只能登记一次且冻结唯一 `UiCommand | PlatformRequest | TaskRegistry` executor与kind级payload上限。`StagedEffect`必须携带exact executor、App/UiRoot/Scope/Node owner scope和deadline，App transaction在model/tree mutation前对照descriptor拒绝unknown kind、lane不匹配、跨owner/过大payload/无deadline；`EffectRequest`出队时直接携带已验证executor/scope/deadline，host无需再按字符串猜测分流，低层手写入口固定归TaskRegistry。AsyncRegistry新增deadline expiration的TimedOut terminal与owner-scope批量取消预检；scope subtree删除会在tree提交前预检completion容量并在同turn取消该Scope effects，root detach覆盖UiRoot/Scope/Node全部effect owner，清理ID随commit/detach结果返回。Rust all-targets check通过且无warning。
- Voplay profile resolver已实现冻结的`core/2d/3d/full/editor`五别名，输出规范Capability集合和required Logic/Asset/Render/Audio role set；`profiles.toml`同步记录默认full、每别名能力/role及禁止依赖。role artifact验证要求exact role set、唯一role、非零schema fingerprint、role匹配placement，并对Render shader ABI与Audio realtime ABI做互斥校验；artifact component audit拒绝Vogui/racing/terrain及各profile越界的3D/physics/audio/editor组件。
- Voplay新增`EndpointGroup`作为InstanceGroupSupervisor与EndpointArena的单一组合owner：start role同时登记supervisor generation和实际driver instance，arena create失败会在任何状态推进前回滚supervisor role；ready、dispatch、wake、poll均先走group role/actor/generation/Running gate再进入arena。shutdown拆为group begin-close与逐role close，driver close成功后才mark supervisor closed并destroy generational instance；任一步失败保留可诊断的Closing/实例状态，避免静默遗失。Voplay三crate workspace all-targets check通过且无warning。
- Voplay clean-slate Vo公共层已建立：根`voplay.vo`保留`Run(Game)`、`NewEngine/Install/Start/StepTicks/Pause/Resume/Shutdown`，Run/Install明确为构建期intrinsic并只向runtime交付GameEntryDescriptor+OwnedInitData；公开Engine/Session均为owner-qualified generational ref。`vo/core|world|schedule|input|assets|scene|render|render2d|render3d|physics2d|physics3d|animation|audio|diagnostics|editor`提供稳定descriptor/ref/command/snapshot类型，没有package global backend、active engine、racing或Vogui依赖。因顶层`vo.work`仍引用已移走旧Vogui manifest，仓库原位check会先被workspace一致性拒绝；将同一新module复制到隔离临时目录后，Vo CLI `check`已通过语法与类型静态检查。
- 新`voplay-codegen` workspace crate读取受治理component schema，校验canonical module/package/type/schema-major、simulation/presentation分类、唯一字段和受支持静态field type；ComponentId由canonical identity SHA-256前128位派生，显式override必须是非零128-bit并与全module ID碰撞检查共用同一registry。生成结果包含component/module fingerprint及每component的强类型Vo value、sparse dense/sparse arrays、added/changed/removed journal、revision和typed query结构；输出按canonical name规范排序，不写工作树。新增crate后的Cargo.lock已离线更新，Voplay四crate workspace all-targets check通过且无warning。
- Voplay `PlatformRenderBackend<A>`把browser WebGPU、native wgpu/WebView主线程等具体实现收敛到同一`PlatformSurfaceAdapter`：attach/resize/rebind/submit/present/detach均保留GameSurface identity、metrics、device generation、fence和pending frame ACK；同Surface只允许一个pending submission，ACK exact匹配后才present，adapter错误明确区分RejectedBeforeSubmit/OutcomeUnknown/SurfaceLost/DeviceLost并映射回SurfaceRuntime恢复域。headless backend继续走同一RenderBackend合同。
- clean-slate `web/`新增严格TypeScript `BrowserSurfaceHost`：每个Engine/Surface/PresentationDomain组合唯一拥有HTMLCanvas、metrics、suspend状态和至多一个pending frame；submit验证pulse/frame/device generation/command budget并复制owned bytes，GPU adapter回包必须携带exact device generation和非零fence，present重验frame/fence后才清pending，deadline miss与device loss为typed outcome。resize禁止跨pending frame，device rebind显式丢弃旧generation pending，detach对outcome-unknown fail closed，close按host owner释放全部canvas binding。复用Studio TypeScript compiler执行strict `tsc --noEmit`通过。
- Vogui clean-slate Vo公共层已建立：根`vogui.vo`固定`Run(App)`、descriptor/init bytes、NewSession/AttachRoot/DetachRoot/Shutdown入口，`vo/core|controls|resources|animation|router|canvas|domunsafe`提供owner-qualified ref、语义控件、UI资源、动画、导航、Canvas与受控raw DOM类型。`profiles.toml`登记冻结的headless/web/native-webview/native-gpu及overlay别名、capability/role/forbidden规则；顶层`vo.work`已移除三个归档旧Voplay示例成员。仓库原位`vo check`通过。
- `vo-app-runtime`新增公共typed PlatformRequest queue并接入SessionKernel/AppSession：Clipboard/File/Navigation/Window/View/VFS/Capability/AudioActivation请求携带Session epoch、caller endpoint、Window/View/Surface scope、严格sequence、deadline与有界payload；完成包保留caller identity并支持按endpoint轮询，避免多framework共享Session时串线。Vogui generated Effect route通过`AppRuntimeEffectBridge`直接lower/submit并按caller收回EffectCompletion；AppRuntime会先验证caller endpoint仍处于允许BeginRequest的状态。Volang、Vogui、Voplay workspace all-target编译检查通过。
- AppSession现公开受SessionKernel治理的Window/View/Surface生命周期。Vogui `HostedUiSession`把UiRoot与`SurfaceKind::Ui`组成attach rollback、可重试detach和shutdown owner；Voplay `HostedVoplayRuntime`把GameSurface与`SurfaceKind::Game`组成attach rollback、detach与shutdown owner。两框架仍保持平级依赖AppRuntime且互不直接依赖。
- Studio新增框架无关`AppCompositionHost`：动态renderer只能以显式Session/Window/View/Surface generational identity和framework owner申请DOM/Canvas layer；host执行唯一Surface、数量上限、layer稳定排序、opaque/transparent/passthrough输入策略、reparent与按owner关闭。`RendererHost`通过声明式`app_surface` capability暴露该入口，Studio继续动态装载framework模块且不解析框架私有render payload。`npm run check`为0错误、0警告。
- App schema compiler现生成TypeScript `AppEnvelopeHeader`、MessageKind wire映射及64-byte envelope encode/decode；codec在payload暴露前验证packet/header上限、magic、major/minor、generational handle、epoch、kind与精确payload length，写入时验证u64/flags并复制owned payload。受治理`app_protocol.ts`与provenance已通过正式generator写回，独立strict TypeScript编译通过。
- App Runtime endpoint packet lane 已从宿主单向轮询扩展为 endpoint-bound 双向可靠通道：每个方向独立执行 exact sequence、packet/count/byte admission，失败 admission 不消耗 sequence；完整 App envelope 在进入 framework decoder 前核对 Session/channel/epoch。HostServices V2 的 `publish_endpoint_packet` 已接真实 kernel callback，并提供按`(CallerEndpoint, framework owner UTF-8 key)`隔离的prebound framework channel；同一AppSession的Vogui、Voplay和其他provider各自分配ChannelHandle/epoch/queue，不再竞争单一poll。AppSession、native event loop、WASM、Tauri、Studio Backend/RuntimeService 与 renderer host 均把动态framework name贯通到open，并可按binding poll/submit。Studio `framework_lane` 在交付 framework payload 前校验受治理 App exact fingerprint 和完整 envelope，回程由 host 写入 authoritative App identity/sequence。`vo-app-runtime` all-target、Studio WASM wasm32、Studio Tauri、Vogui/Voplay feature checks与Studio TypeScript检查均通过。
- Vogui 正式 Web 双向链已接入生产 owner：`HostedUiSession::open` 直接拥有 `AppRuntimeUiLane`，每个 Root 保留 lane 背压下尚未 admission 的 patch/snapshot，safe point 有界消费同一 UiReturn lane 的 ApplyAck/Event/UiCommandResult并发布 presentation 与 command。Vogui protocol 新增 bounded UiCommand binary codec；DOM renderer 经 Studio `framework_lane`处理 patch/snapshot/command，命令支持 focus/blur/scroll/measure/selection/composition cancel和明确 unsupported terminal。measure result现完整携带 logical NodeRef/binding generation、metrics/layout revision、CSS viewport坐标空间与 milli-pixel几何值，Rust回程重验当前binding。`vo.mod`显式声明`framework_lane`；`vogui-runtime --features app-runtime --all-targets`离线编译与JS strict TypeScript检查通过。
- Voplay 正式 Web frame lane 已完成首个生产接线：Voplay protocol 新增 `SurfaceControl`、`FramePulse`、`DeviceEvent` 的有界二进制payload codec，携带App Session/Window/View/Surface、Engine/PresentationDomain、render endpoint/device generation、pulse/frame/deadline、render/control/simulation revision、graph signature、commands、fence与terminal outcome。`SurfaceRuntime`新增单pending remote frame admission/completion，RejectedBeforeSubmit不消耗FrameId，Presented/Deadline/SurfaceLost/DeviceLost/OutcomeUnknown分别进入结构化状态；`VoplayRuntime`和`HostedVoplayRuntime`可attach远端Surface、生成frame、保留lane背压重试并在safe point核对inner sequence/commit/revision后完成ACK。clean-slate Voplay Studio renderer通过`app_surface + framework_lane`创建Canvas layer，Canvas2D执行portable Clear/SolidRect frame并回送Presented/DeadlineMissed/Suspended/SurfaceLost/DeviceLost/RejectedBeforeSubmit/OutcomeUnknown；device rebind清理旧pending fence。`vo.mod`已声明renderer artifact和两项capability；`voplay-runtime --features app-session --all-targets`离线编译、Voplay Web strict TypeScript及Studio `npm run check`（0错误、0警告）通过。当前通道只取得编码与静态编译证据，真实browser/native组合和多framework独立lane认证仍pending，因此未创建EVID。
- App Runtime 已新增 Session-owned display scheduler：View timing request 按 View coalesce，PresentationDomain 以 owner+Engine+Domain 精确索引并保存 Surface/render/logic route、visibility、metrics/timing revision和frame budget；每 domain 只有一个 latest-only pulse，覆盖会记录 coalesced count，hidden/suspended domain 不消耗其他 domain credit。HostServices V2 `request_display_pulse` 已从 unavailable callback 切到真实 kernel admission；Surface/View/endpoint/session close 会撤销对应 timing request、domain route 与pending pulse。
- Voplay Web Surface attach 现同步登记 `PresentationDomain -> View/Surface/render route`，由 App Runtime 分配 per-domain PulseId/deadline，再通过 scheduled pulse 路径直接驱动 Voplay presentation；coalesced pulse 允许单调 PulseId 跳跃。attach 自动申请首个 pulse，后续 frame safe point继续申请；显式 Web detach 会先确认 lane admission 和 GPU outcome 已终结，发送 SurfaceControl Detach、撤销 domain route，再关闭 runtime/App Surface。Studio WASM 使用 `requestAnimationFrame` 提交微秒 timing，native WebView 经 Tauri command 和 VM owner-thread event提交同一 typed request；`vo-app-runtime`、Studio WASM、Studio Tauri、Voplay app-session feature与Studio TypeScript必要检查通过。
- Voplay Web SurfaceControl 生命周期已补齐 Resize/Suspend/Resume/Rebind：Hosted owner 在无 pending control/frame 时同步修改 SurfaceRuntime 与 App Runtime PresentationDomain route，递增 metrics/timing revision，任一 lane admission 或 route update 失败会执行反向恢复；visibility 变化和 route revision 更新会清除旧 display pulse。renderer/device recovery 先冻结并更新全部 Web route，成功后将逐 Surface Rebind 保留在有界于 Surface 数量的控制队列，控制消息必须先于新 frame 进入 framework lane。浏览器 renderer 每次控制变更验证 owner route，frame 提交核对当前 Window/View/render endpoint/device generation；共享 GPU 的多个 Surface 可在同一新 device generation 下依次 rebind。`vo-app-runtime --all-targets`、`voplay-runtime --features app-session --all-targets` 离线编译和 Voplay Web strict TypeScript 检查通过；真实浏览器恢复 smoke 留到全量编码完成后的统一验收。
- `runtime_plan` 已从仅保存plan identity/providers的骨架扩展为冻结的 `AppBuildPlan -> TrustedHostProbe -> ResolvedAppRuntimePlan`：build/entry/schema/App protocol fingerprint、有限认证variant、target/host topology、probe requirement/certification、exact materialized artifact及detached manifest/trust evidence、provider placement、initial graph、capability grant和effective limit全部进入不可变计划。Development/Release trust policy、非法target/topology、缺失provider artifact、未认证placement、重复identity/capability、未授权动态group capability会在factory/guest entry前拒绝；SessionKernel保留完整resolved plan并禁止同Session二次安装。
- `vo-web` 的 BrowserArtifactPlan 现有正式执行后物化阶段：对 local build output 与 ready-module artifact 统一解析实际文件，执行 regular-file/非空/大小/唯一 source path 检查，从真实字节计算 SHA-256 content digest；规范化 module/extension/family/role/AssetRoot/path/runtime roles 形成 artifact identity，并产生长度前缀 detached manifest、manifest digest和development attestation digest。Native Studio 在启动 guest 前完成该物化，把结果与对应 preview Session 一起保留并执行数量上限复核，后续 AppBuildPlan variant 不再需要从路径或声明猜测 artifact digest。`vo-web --all-targets --offline --locked` 冷编译14m44s通过，Studio Tauri all-targets检查无warning通过；下一步仍需将这些记录连同entry/host provider事实组装为正式variant并关闭unplanned session入口。
- Native 与 Browser Studio 的生产 guest 启动现已统一收口到正式 `ResolvedAppRuntimePlan`。Native materializer 对实际 entry bytecode、当前 Studio executable、extension WASM/JS glue 与 framework JS module计算SHA-256并组装Development trust variant；Browser loader单次读取`vo_studio_wasm_bg.wasm`字节，以同一`Uint8Array`初始化wasm-bindgen并向Rust登记host digest，`compileGui`同时从VFS冻结extension/framework artifact materialization并签发一次性launch token。Browser启动必须消费exact token并核对entry path和bytecode digest，随后以BrowserWasm/BrowserMain variant安装SessionVm provider catalog、factory manifest、loaded factory、initial group和完整framework capability grant；resolved plan与artifact records由对应preview owner保留。自由capability的hosted Session构造器已退出生产API，仅作为crate内测试适配编译。Native/Browser共享同一artifact identity、detached manifest和development attestation算法；`vo-web --all-targets --offline --locked`、`vo-app-runtime --all-targets --offline --locked`、Studio WASM wasm32 all-targets、Studio Tauri all-targets及Svelte/TypeScript检查均通过且无warning。当前首个planned Session边界已闭合；framework renderer/provider仍由Studio动态模块owner装载，后续需将其升级为ProviderInstance lifecycle并补齐真实platform probe/certification。
- Studio framework 模块现已进入正式 ProviderFactory/ProviderInstance 生命周期。Browser/Native variant identity完整绑定provider template、factory requirement、manifest、trust evidence与loaded状态；framework factory在计划安装时只到ManifestVerified，JS模块成功import后才由guest owner验证为LoadedValidated。Browser与Native均按`load -> DynamicInstanceGroup -> Prepare -> Start -> Ready/finalize -> close -> safe unload`执行，失败路径回滚pending group，预览关闭先关闭active group再卸载factory。Native全部操作经guest event loop owner thread串行；Browser启动拆为planned Session prepare与VM start两阶段，host-bridge模块在Session建立后加载并保持provider pending，VM首帧完成后由renderer初始化推进Ready，消除了Session建立前执行framework host模块的入口。`vo-app-runtime --all-targets --offline --locked`、Studio WASM wasm32 all-targets、Studio Tauri all-targets及Svelte/TypeScript检查通过，前端0错误0警告；真实平台probe、live-count观测与故障/浏览器运行验证按编码完成后的统一验收顺序保持pending。
- 共享`vo-schema-compiler`现提供generator identity、schema/source fingerprint、toolchain/target/capability归一化cache key、原始schema source-span稳定诊断和受限package-relative artifact模型。Volang新增正式`vo generate [path] [--target] [--write]`入口及`vo.generator-provider/1`握手：provider必须attest精确name/version/schema kind，默认输出进入工作树外immutable content-addressed cache，只有显式`--write`写回项目；输出声明缺失、路径逃逸、provider identity漂移和cache collision均失败。`vo build`发现`vo.generate.toml`后会在依赖安装完成、包收集与类型分析前运行provider，并把生成`.vo`直接注入同一次immutable compile snapshot；已跟踪同名源仅在字节完全一致时接受。Vogui新增`vogui-codegen` workspace crate与`vogui-generator-provider`，schema固定typed Model/Message/Init、named callbacks、transaction mode、limits、ScopeBuilderId/Mapper/subscription表，拒绝closure、borrowed memory、live object expression与重复ID；输出稳定App/Artifact/model/message/transitive ABI fingerprint、factory id、AppEntryDescriptor、scope/mapper table、provider manifest与init/model/message类型绑定。Volang `vo --all-targets --offline --locked`和Vogui workspace all-targets离线锁定检查通过且无warning；完整字段级init/model/message二进制codec、generated AppDriver方法体、extension artifact自动解析及`check/run/test`构建前注入继续进入下一编码切片，尚未创建EVID。
- Vogui typed generator已继续补齐字段级owned codec与具体App glue：Init/Model/Message和每个mapper payload只接受bool/i32/u32/i64/u64/string/bytes有界字段，decoder拒绝截断、非法bool、长度越界和trailing bytes；生成`vogui.App`具体实现、factory、entry descriptor及ScopeBuilder/Mapper/Subscription静态dispatch，callback只能是同package named symbol。Volang的build/check/run/test/emit现共享同一generation-before-analysis入口。模块公共清单新增多`extension.generator` provider与per-target `extension-generator` artifact；省略开发期executable时，CLI先物化locked graph，再从认证过的installed module extension contract解析精确provider并验证release artifact。Vogui `vo.mod`现声明`vogui.typed-app@3`的五个首批native target artifact identity。`vo`与`vo-web`全target锁定离线检查无warning通过。
- Vogui native GPU路径已从runtime物理拆为`vogui-layout`、`vogui-text`、`vogui-native-accessibility`、`vogui-native-renderer`四crate。layout owner按tree diff把dirty传播到最近formatting root，执行有界intrinsic text/image measure并提交revisioned box snapshot；text owner以font/style/width/locale/direction/font-generation为完整cache key并由host注入font resolver/shaper；native accessibility对SemanticSnapshot执行prepare/commit transaction并提供per-UiRoot macOS sink boundary；native renderer实现layout→text→hit index→paint list→device-generation painter submit、poison hide、device rebind与显式close，未导入Voplay RenderGraph。profile resolver新增exact role artifact校验和同源BuildRecipe，明确列出每个alias的Vo package、Rust crate、JS chunk与role，native四crate只进入retained-gpu组合，DOM chunks只进入DOM组合。Vogui七crateworkspace all-targets锁定离线检查无warning通过；具体CoreText/AppKit/wgpu adapter、IME平台接线和真实macOS认证仍按编码阶段继续。
- Voplay 已建立精确 capability/role 物理产物边界：workspace 新增 `voplay-assets`、`voplay-render-core`、`voplay-render-2d`、`voplay-render-3d`、`voplay-import-gltf`、`voplay-physics-2d`、`voplay-physics-3d`、`voplay-animation`、`voplay-audio` 与 `voplay-extension`。native asset owner 将 Engine/provider generation/actor/byte budget 绑定到每次 I/O；native render owner 将 Surface、device generation、submission fence 与 rebind/detach 串行化；native audio owner 完整持有 AudioDevicePermit 的 activate/rebind/callback/deactivate 生命周期。runtime feature 集可独立编译 core、2D、3D 和 full，editor 只通过显式 editor feature 进入。BuildRecipe 冻结 capability、role、Vo package、Rust crate 与 Web worker chunk，RoleArtifact 进一步绑定 artifact name、target、digest、schema、shader/realtime ABI 和完整 capability 集；`voplay-extension::launch` 在构造 Game 前完成精确 materialization，并以 owner wrapper 保证 Engine shutdown。
- Voplay component/store/query generator 已升级为正式 `vo.generator-provider/1`：`voplay.component-store@12` 对外证明 schema kind，使用共享 generator identity/schema/toolchain/target/capability cache key，原子写入受限 package-local source path 与工作树外 manifest path，并生成 component source、Game entry 与 governed manifest。`vo.mod` 已声明五个首批 native target 的 extension generator artifact，仓库新增可直接复制的 schema 与 `vo.generate.toml` 示例。Voplay 整 workspace、core-only、2D-only 与 3D-only 离线 all-targets 编译检查均通过；这些仍属于编码期静态检查，未创建 EVID。
- Voplay generator 的 Game 路径已补齐：同一 governed schema 可冻结 Game/Init 类型、named Configure/Start callback、max init bytes 与精确 role set，输出 typed owned init binary codec、generated Game adapter、GameEntryDescriptor、factory id、artifact/schema/role-artifact-set fingerprint 和 manifest 记录。`voplay-extension::launch` 消费 materialized role artifact set 后才构造并 Start Game，owner wrapper 的显式 shutdown 与 Drop cleanup 共用 GameEngine 完整关闭链。
- 共享组合层现支持真正的多 layer pointer hit stack：CompositionRegistry 暴露每个 View 按 z-order 排列的 SurfaceLayer，pointer stack 会让 transparent/passthrough UI delivery 继续到下层 Voplay Surface，同时保持 pointer capture、keyboard focus、text/IME owner 和关闭时 focus restore。Hosted AppSession 已公开 layer 查询、focus、capture、IME 与仲裁入口；Studio AppCompositionHost 可按坐标返回 top-to-bottom Surface identity stack，Svelte/TypeScript 静态检查 0 error/0 warning。
- `gpu-native-host` 新增通用 `NativeCompositorOwner`：同一 View 每 pulse 只接受一个、同 device generation、Surface 唯一、z-order 稳定的 layer frame，submit/present 绑定 exact fence，pending 时拒绝 rebind/detach，关闭按反向 owner 顺序释放 View。Vogui NativePainter 与 Voplay NativeRenderDevice 均返回 texture token/content revision/device generation/fence；独立 `voplay-vogui` 集成 crate 只负责把两框架输出组装成 native composition frame 和 pointer hit stack，未进入 Voplay full recipe。Volang app runtime、Vogui workspace、Voplay workspace 与该集成 crate 的离线 all-targets 编译检查全部通过。

这些结果尚未满足完整 TEST-PROTO-001 和 TEST-RT-001；ASan 与剩余 decoder/恶意大小 fixture、浏览器真实运行时集成测试、child-process adapter parity、Vogui timer/tick 与 package-level handler registry 重写、`vo-ext` 过渡 shim 删除、provider unload/inflight gate 与完整 shutdown 仍待实现，因此未创建 EVID，ACC 保持 planned。

## 授权边界与 pending 项

- 已授权本地源码、协议、生成器、测试、受治理生成物和构建系统修改。
- commit、tag、branch、push、PR、发布与部署未获授权。R0 的三仓 commit/tag 和第 21 章 rewrite branch 保持 pending。
- Vogui/Voplay sibling owner 源码继续在各自仓库直接修改；当前本地写入路径可用。

## 下一关键路径

1. 补齐 Vogui CoreText/AppKit/wgpu/IME 具体 native adapter、profile artifact 物化与 macOS 生产宿主接线。
2. 补齐 Voplay GameEntryDescriptor/init codec 的生成器输出、各 profile 发布 artifact 表与具体 wgpu/CoreAudio 平台 driver。
3. 将 browser hit stack 接入 framework lane，并补齐具体 wgpu/CoreAudio/CoreText/AppKit driver 与 macOS host window/IME 接线。
4. 完成全部预定编码后，统一执行恶意 decoder、跨语言 golden、浏览器/native smoke、故障恢复、性能、ASan 与 86 项 ACC/EVID 验收。

当前未创建 EVID，未将任何 ACC 标记为 verified/accepted。未提交、未推送、未发布。

## 2026-07-24 编码完成后的统一审阅与验证

冻结计划中的功能编码阶段已经结束，工作进入统一审阅、回归和交付门收敛。此前“下一关键路径”中列出的 CoreText/AppKit/wgpu、CPAL、Web framework lane、provider lifecycle、Voplay role crate、组合宿主和恢复模块均已落入当前源码并参与工作区编译；旧条目仅保留为实施历史，不再代表当前待编码清单。

统一审阅期间修复了发布测试与 Web runtime 仍使用旧 provider-role manifest 的迁移遗漏、VM host-event wake 绕过 RuntimeCommand 边界、island scheduler 源契约过度依赖旧语法形态、发布/布局治理配置漂移、fuzz target artifact 声明缺失，以及 Studio WASM 使用但 App Runtime 未公开 `GenerationalHandle` 的真实目标编译错误。三仓 Rust 源已统一格式化，三仓 `git diff --check` 通过。

已通过的本地验证包括：

- Volang `cargo test --workspace`、`cargo check --workspace` 和 `cargo test -p vo-dev --locked`；
- Vogui、Voplay 各自的 `cargo test --workspace`、`cargo check --workspace`；
- App protocol 与 framework protocol fuzz target 的锁定编译；
- Studio WASM 正式构建、Tauri 全目标编译、Svelte/TypeScript 检查、站点契约测试和生产构建；
- `vo-dev` 的 traceability、repo-boundaries、layout、docs、skill、studio-web、examples、benchmarks、release lint；
- 真实浏览器中从 Studio 首页进入编辑器、加载新 WASM、编译并运行 channels 示例，VM 输出完整且页面进入 done 状态。

普通消费者生成链的复审使用两个工作树外应用分别依赖 Vogui `headless` 与 Voplay `core` profile，实际执行 provider 握手、`vo work sync`、内容寻址生成和 `vo check`。该路径修复了生成源码被二次 authority closure 误拒、生成 `.vo` 错放 `generated/` 子包、合法带点号/连字符模块名被 generator 拒绝、Vogui typed adapter 缺少 `ViewBuilder.Style`、空 scope/subscription schema 产生未使用局部变量，以及 extension generator 版本元数据漂移。Volang 现在只豁免本次明确注入的新增路径，已跟踪同名源码仅在字节完全一致时接受；不同内容得到生成阶段冲突诊断。Vogui generator v3 与 Voplay generator v12 的 `.vo` 输出进入声明包目录，manifest 保持工作树外缓存布局；两条普通 build 均未向应用目录写入生成物。上述结果用于 `TEST-PROTO-003` 的实施记录，尚未创建 EVID。

仍受未提交状态约束的交付门：

- `lint artifacts` 要求 checked-in App protocol 生成物已经进入 Git 索引；当前文件内容已生成并通过消费方测试，但保持 untracked；
- `lint studio-tauri` 要求 `vogui-protocol` 指向 Vogui 新源码对应的精确 Git revision；当前 Vogui 重写没有 commit，因此不存在可合法填写的 revision；
- Quick Play 归档生成器只从四个固定 Git revision 的索引构建。现有归档仍含旧 Voplay manifest，当前未提交的 Vogui/Voplay 全量源码无法进入新的不可变归档。

这些门需要先为 Vogui、Voplay、Volang 建立正式 commit 身份，再更新消费方 pin、重建 Quick Play 与 checked-in artifact，随后才能创建绑定 commit/artifact digest 的 EVID 并推进 ACC 状态。当前继续遵守用户边界：未提交、未暂存、未推送、未发布，也未伪造 EVID。

## 2026-07-26 TEST/EVID 收敛

`eng/rewrite-validation.toml` 现为全部 32 个 TEST ID 提供可执行命令和必需制品声明；`vo-dev rewrite validate --case <TEST-ID>` 会执行命令、记录耗时与退出状态、计算必需制品 SHA-256，并记录 Volang/Vogui/Voplay 三仓 commit 与 dirty identity。工作树未形成稳定 commit 时报告明确保持 `certifiable=false`。

本轮已实际执行 App Runtime、三套协议生成、profile、Engine、Asset、Render、Input、Recovery、Multiview、RenderFeature、稳定场景基准、World、Audio、Vogui runtime/增量树/事件/资源/widget/render、跨仓边界和完整交付矩阵。真实 Chromium 中 Vogui DOM smoke 通过，Voplay WebGPU/Canvas2D/Haptics/provider restart/Asset/Audio gesture 六项通过；宿主 macOS 下 Voplay–Vogui 组合窗口呈现三帧并提交 accessibility revision，CoreAudio 完成 active/recovered/settled callback 且零 callback error。

协议验证现包含两个可复现的 coverage-instrumented libFuzzer campaign：App envelope/Channel/optional-section decoder 与 Vogui/Voplay framework decoder 各使用固定 seed `424242`、最大输入 4096 bytes、100,000 runs，均通过。普通消费者 install smoke 已固化为 `eng/run-rewrite-consumer-smoke.sh`：每次创建全新临时 workspace，构建 Vogui/Voplay generator provider，分别以 `headless`/`core` profile 执行 `vo work sync`、内容寻址生成和 `vo check`，并确认 governed source/manifest 未写回应用树。该 smoke 同时发现并修正 Vogui v4 示例清单仍声明旧 generator 输出文件名的问题。

profile 体积与依赖树审计已固化为 release WASM 可复现构建。Voplay `core/2d/3d/full` 的 raw bytes 分别为 `448169/1182591/2847922/3331813`，确定性 gzip bytes 为 `134260/323957/727358/865036`，均低于冻结的 `250 KiB/900 KiB/1.5 MiB/1.8 MiB` 上限；每项同时记录 Brotli、artifact SHA-256 和 dependency-tree SHA-256。Vogui `web-minimal/web-full` 首轮 release WASM raw bytes 为 `1216670/1217610`，gzip bytes 为 `339276/339796`，继续按第 16.5 节保持预算待冻结。完整 `TEST-PROFILE-002` 通过。

App Runtime 全量测试曾发现平台认证测试夹具同时把 GPU 声明为必需并期待 `UnexpectedObservation`；夹具已改为移除 Voplay workload 与 GPU requirement 后再验证 fail-closed，随后 `TEST-RT-001` 全部通过。

追踪 lint 现强制：

- 每个 TEST 都有且仅引用真实可执行 validation case，REQ/TEST 双向链接一致；
- 每个 ACC 至少直接引用一个 TEST，且 ACC/TEST 共享 owning requirement；
- EVID 使用合法 commit SHA、artifact SHA-256、target/toolchain/time/duration/result 元数据；
- browser/native/device smoke 包含 operator、device OS 与附件；
- verified/accepted ACC 的每个 TEST 都有 passing direct EVID；
- verified/accepted REQ 覆盖全部 required evidence kinds、全部 TEST 和对应 ACC 状态。

14 条最终交付 ACC 已补齐 composition、recovery、input、browser、native device、CI、profile、protocol、boundary 与 delivery TEST 引用；既有跨 requirement 的 ACC/TEST 引用也已补齐反向 owner 关系。traceability lint、相关 `vo-dev` 单元测试与三仓 `git diff --check` 通过。

当前唯一已复现的聚合 TEST 失败为 `TEST-CI-001`：`lint artifacts` 拒绝尚未进入 Git 索引的 App protocol 生成物。`TEST-DELIVERY-001` 的文档、traceability、release matrix、Studio、Vogui/Voplay 全工作区全目标检查通过。最终 EVID 和 ACC 状态继续等待三仓稳定 commit identity；未暂存、未提交、未推送、未发布。

## 2026-07-25 统一运行验收继续收敛

- App Runtime、Vogui 与 Voplay 新增统一 instrumentation、零泄漏摘要、确定性 fault injection、build-identity-bound profile report 和平台认证报告。故障规则的 skip/every/remaining、容量、耗尽与移除语义已有定向测试；SurfaceLost 和 RendererLost 已接入真实 mutation 入口，验证注入失败不改变 owner 状态，规则耗尽后的下一次操作可恢复。App Runtime composition shutdown 与 Vogui root detach 均由 live leak summary 证明归零。
- App Runtime 新增 1,000 次完整 Session owner graph create-close soak。每轮都创建 App Channel、FrameworkLogic/AudioControl/AudioRealtime endpoint、request 与 timer、bulk buffer、wake registration、AudioDevice lease、Window/View/Game Surface；进入 Closing 后验证 reserved `PlatformCompletion` 继续接受、普通 framework ingress 被拒绝，随后统一释放所有 owner 并推进到 Closed。实际执行 1,000 轮通过，最终每轮 `SessionLeakSummary` 的 channel/request/endpoint/composition/display/buffer/wake/timer/audio/provider/input/diagnostics/packet 全字段均为零。
- Vogui 新增 1,000 次 UiSession owner graph attach-detach soak。每轮使用独立 UiSession identity 创建 UiRoot、retained button tree、NodeRef/EventToken、pending UiCommand、root-scoped Effect/Subscription、session-owned Image resource 与 root lease；detach 会返回 command terminal、Subscription Stop 和 cancelled Effect ID，App safe point 再取得精确 `EffectOutcome::Cancelled` completion，旧 NodeRef 被拒绝，最后 unregister session resource。实际 1,000 轮通过，`UiSessionLeakSummary` 的 root/node/presentation/return/command/effect/completion/subscription/resource/lease/fetch/ref/reload 全字段归零。
- Voplay `RenderWorld::apply_delta` 已去掉小增量路径上的整 World clone，通过 staged decode、全量容量预检和原位提交保持失败原子性；10,000 对象稳定场景每轮只解码和排队一个对象。Vogui 单 Scope reconcile 新增 scope path 索引和局部 staging/apply，10,000 节点稳定树每轮只扫描目标节点。
- 两个稳定基准现支持有界环境变量运行次数，并在 100,000 轮 soak 中通过累计工作量断言。Vogui 结果为 p50 10,750ns、p95 18,125ns、p99 19,541ns，节点保持 10,000；Voplay 结果为 p50 1,042ns、p95 1,542ns、p99 4,209ns，对象保持 10,000。
- Vogui 与 Voplay 的真实浏览器 smoke 已运行。Voplay 现经正式 `WebGpuCanvasAdapter` 与 `BrowserSurfaceHost` 覆盖 Chromium WebGPU 双 Session/双 Surface 的 first-frame 与连续三帧 current-texture、command encode、queue submit 和 present；暂停 primary Surface 后 peer Session 仍可独立 resize/submit/present，尺寸互不污染。同时覆盖 Canvas2D pixel、Surface 生命周期、haptics terminal outcome、provider stop/restart、asset lifecycle 和用户手势 WebAudio activation；全部 case 通过。smoke 按钮改为监听器就绪后才启用，消除了提前点击造成的伪超时。Vogui 覆盖 retained DOM 样式、状态 stylesheet、role、aria-label 与 tab order；该 smoke 捕获并修复了 `SetProperties` 替换后旧 role/ARIA/tabIndex 及其他受控 DOM 属性残留的问题。
- Chromium WebGPU smoke 进一步执行真实 controlled device loss：在存在 pending frame 时销毁当前 `GPUDevice`，确认旧 device 停止接收提交；随后重新申请 adapter 与 device、推进 generation，旧 frame 得到 `deviceLost` terminal，新 generation 恢复 present。首次运行暴露 Dawn adapter 单次消费合同，旧实现错误地在 consumed adapter 上二次 `requestDevice()`；`WebGpuCanvasAdapter` 现把 navigator GPU 作为恢复 authority，每次 device loss 都重新申请 adapter/device，并原子替换两者及 pipeline/texture/surface residency。
- 新增 `voplay-vogui` 的 `macos_gpu_smoke` 可执行示例，并在本机真实运行：创建 640×360 AppKit Window、CAMetalLayer/WGPU Surface 和 Metal device，设置 IME caret，向真实 AppKit sink 提交 revision 1 的可操作 Button 语义树，再 dispatch `Press` 并从有界 action channel 收回相同 root/revision/node/action；注册纹理层后以连续递增的 pulse/content revision 提交并 present 三帧，三帧均为 `Presented`，随后按 topology→window 顺序关闭。首次运行捕获 compositor uniform binding 缺少 vertex visibility，修复后真实窗口 smoke 通过；当前输出 `presented_frames=3, accessibility_revision=1`。
- 新增 `voplay-audio` 的 `cpal_smoke`，使用默认 macOS 输出设备和静音 PCM ring 执行真实 CPAL/CoreAudio 生命周期。generation 1 激活后取得硬件 callback，generation 2 rebind 会关闭旧 stream 并恢复新 stream callback，最终显式 close 后 callback 计数稳定且 callback error 为零；本次输出 `active_callbacks=4, recovered_callbacks=10, settled_callbacks=10, callback_errors=0`。`CpalAudioDevice` 新增只读 callback invocation instrumentation，不向实时线程增加锁或分配。
- 当前收口回归为 App Runtime 105 tests、Vogui runtime 66 tests、Voplay runtime 179 tests 全部通过；Vogui/Voplay Rust 工作区全目标编译和测试、两套 TypeScript strict check、三仓 rustfmt 与 `git diff --check` 通过。Volang `vo-engine` 在不污染其独立 native target 目录并允许 loopback/临时非 UTF-8 fixture 的原始环境下 222 tests 全部通过。
- App protocol 与 framework protocol 的 coverage-instrumented libFuzzer target 均已用离线锁定依赖完成 release 重建，并以固定 `seed=424242`、`max_len=4096` 各执行 100,000 次。两组 campaign 都以 0 退出，分别新增 88、434 个 corpus unit，峰值 RSS 分别为 25MB、27MB，没有产生 crash artifact。当前 stable Rust 环境只覆盖 sanitizer coverage；ASan 仍要求带对应 sanitizer runtime 的工具链，未把本次结果冒充 ASan 证据。
- Voplay 新增 1,000 次四角色 GameEngine owner graph create-close soak。每轮启动 Logic/Asset/Render/Audio endpoint，创建 World entity，并同时保留 endpoint event、Logic I/O command、durable render transaction inflight、后续 pending render op、transient presentation domain、PresentationState value 和 SimulationState resource/event。审阅首先确认旧关停路径只把 endpoint 标记为 Closed，仍由 Engine 容器持有角色、实体和队列；现已在 Shutdown stage 完成及 supervisor 全部 Closed 后统一释放 supervisor/control store、endpoint/actor、World、RenderOutbox、PresentationState、SimulationState、LogicIo、PluginRegistry 和 pending event。增强后的 1,000 轮用例通过，每轮 Closed snapshot 的角色、实体、endpoint event/bytes、Logic I/O、acked/pending/inflight/snapshot/transient render 状态均归零；随后 Voplay runtime 完整 171 tests 全部通过。
- VoplayRuntime 外层另执行 1,000 次完整 create-close：每轮通过正式 `run` 创建四角色 Engine 与 DeviceHub lease，同时 attach 一个 headless Surface 和一个 remote Surface，对应两个 InputScope。该用例捕获了 shutdown 清空 Surface 集合却没有注销 InputScope 的真实泄漏；修复后每轮 runtime surface/remote surface/input scope、GameEngine owner 与 DeviceHub engine binding 均归零，完整 `VoplayLeakSummary` 为零。异常路径复审还捕获 Game Shutdown hook 返回错误时提前跳过 DeviceHub detach，以及 RenderBackend detach 失败后 Surface 停留在 Closing 并中断其余 owner 回收的缺口；关停现在隔离失败 Surface、继续释放 InputScope/Game/device lease、封闭 Runtime，并向调用方保留首个 BackendRejected 或 GameFailure。成功与两条失败关停用例加入后，Voplay runtime 完整 174 tests 全部通过。
- `ACC-PLAY-024` 的 Runtime 层共享 DeviceHub 路径新增双 Engine 集成用例。两个四角色 Engine 各自 attach headless Surface；Engine A renderer fault 只推进 A 的 endpoint generation，Engine B 的 binding/device generation 保持不变并在 A 恢复期间成功 present。随后共享 DeviceLost 同时把两者推进 AwaitingDeviceRebind；A 先完成时 Hub 仍为 Recovering，B 完成后才回到 Ready，两者在 device generation 2 上继续 present，最终共同 shutdown 后 Hub binding 归零。Voplay runtime 完整 175 tests 全部通过。
- 共享 DeviceLost 的恢复/关闭交叉路径又捕获 `DeviceHub::detach_engine` 的状态收敛缺口：一个 Engine 已完成 rebind、另一个仍 AwaitingDeviceRebind 时，后者直接 shutdown 会删除 binding，但旧实现不会重新计算 Device 状态，Hub 永久停在 Recovering。detach 现在会在剩余同 Device binding 全部 Ready 时原子推进 Device 为 Ready；Runtime 集成用例证明等待中的 Engine 退出后，存活 Engine 可立即重新 attach Surface、present，并最终释放全部 binding。Voplay runtime 完整 176 tests 全部通过。
- `ACC-PLAY-010` 新增 scheduled remote PresentationDomain 隔离用例，并捕获 suspended Surface 的 pulse 入口与 frame terminal 语义断裂：`prepare_remote_frame` 已实现 `Suspended` terminal，旧 `begin_scheduled_pulse`/`begin_exact_pulse` 却提前返回 InvalidState，使该 terminal 永远不可达。pulse admission 现允许 Active/Suspended 两种状态并继续推进单调 PulseId；suspended domain 得到正式 `Suspended` terminal，同 Engine peer domain 同时生成 submission、完成 exact ACK、清空 pending，随后 suspended domain 可 resume。Voplay runtime 完整 177 tests 全部通过。
- remote Surface 关停路径新增 owner-only close。普通 `detach_remote` 继续要求 pending remote frame 已取得 terminal completion；完整 Runtime shutdown 则可显式放弃仍在途的 remote submission，直接移除 Surface/domain record，再释放 InputScope、Game owner 与 DeviceHub lease，不再把正常关停误报为 InvalidState。集成用例在 pending submission 未 ACK 时执行 shutdown，最终 runtime surface/remote surface/input scope、Game roles 和 Hub binding 全部归零。Voplay runtime 完整 178 tests 全部通过。
- Voplay profile 顶层 owner 的 Drop 路径捕获同步 guest 回调违规：`ProfiledEngine::drop` 旧实现直接调用 `GameEngine::shutdown()`，会执行用户 Game Shutdown system。GameEngine 现把用户 Shutdown stage 与 runtime owner cleanup 拆分为显式 `shutdown()` 和无 guest 回调的 `abandon()`；两者共用同一角色/endpoint/control/world/outbox/state/queue 清理函数并对 Closed 幂等。ProfiledEngine 正常 shutdown 仍执行一次用户 hook，Drop 只调用 abandon。定向用例证明连续 abandon 不执行 hook且 owners 归零，连续显式 shutdown 只执行一次 hook。Voplay runtime 179 tests 全部通过，`voplay-extension` 完整 profile 依赖闭包测试与 doc-test 编译通过。
- AppSession 组合层的 `HostedVoplayRuntime::shutdown` 同步修复提前返回：旧实现任一 web/native Surface detach 失败都会跳过其余 Surface、底层 Runtime/DeviceHub 和 Provider group close。当前路径保留首个结构化错误，同时继续尝试全部 Surface detach、Runtime shutdown 和 group close；因此局部 AppSession/remote-frame 错误不再阻止 Engine 与 provider owner 收敛。`voplay-runtime --all-features --all-targets --locked` 已覆盖 `app-session` 正式组合编译并通过。

以上均为当前未提交工作树上的真实本地执行结果。由于用户仍未授权 commit/tag/push/发布，结果没有稳定 commit identity，继续不创建最终 EVID，也不把 ACC 标记为 accepted。

## 2026-07-26 浏览器组合与最终认证生成器

- 新增真实 Chromium 组合 smoke：Voplay `BrowserSurfaceHost`/`Canvas2dGpuAdapter` 与 Vogui `DomRenderer` 在同一 App View 中同时呈现，验证 retained DOM/semantics overlay、typed hit-region UI/game pointer 仲裁、modal 下层输入暂停与恢复，以及反向 owner shutdown 后 shared surface 归零。该 smoke 已纳入 `TEST-COMPOSE-001`，连续独立运行两次通过。
- Vogui 浏览器 smoke 现通过真实 `PointerEvent` 和 renderer delegated listener 验证 typed `UiReturn` 的 event/revision/token，同时保留 DOM 属性替换与 renderer-owned interaction stylesheet 检查。`TEST-GUI-RENDER-002` 与 `TEST-GUI-EVENT-001` 均通过；后者同时通过 Rust input/command/router/inspection 与真实 macOS GPU/accessibility action smoke。
- `eng/rewrite-validation.toml` 的全部 validation case 已声明 evidence kind；traceability lint 会验证 TEST、REQ、ACC 与 evidence kind 的直接覆盖关系，并要求 browser/native/device/real-platform 类别具有真实附件。
- `vo-dev rewrite certify` 会核对三仓当前 clean commit、32 份报告的同一 source identity、当前 manifest 命令、通过状态、toolchain/operator/device、附件清单及 SHA-256，随后生成稳定 EVID ID 与 certification bundle。新增 `--traceability-output <path>` 可在全部 gate 通过后生成回填 EVID、evidence refs，并将 REQ/TEST/ACC 收敛为 accepted 的完整 traceability 文件；任何 ACC 的任一 TEST 缺少 direct EVID 都会拒绝生成。
- 当前三仓仍为 dirty worktree，因此认证命令按设计拒绝生成最终 EVID。未暂存、未提交、未推送、未发布。

## 2026-07-26 原生组合宿主验收补强

- Studio 新增真实 `webview-native-host` 验收驱动。它由 Tauri 启动首个 WKWebView，实际完成 native→WebView 与 WebView→native typed bridge 帧传输；随后 native owner 推进 bridge epoch、保存 UI model 与 SimulationWorld 快照、关闭首个窗口并创建第二个 WKWebView。第二代 WebView 成功附着 epoch 2、恢复两份快照、拒绝 epoch 1 旧输入并把恢复 completion 送回 native。结果已写入 `target/rewrite-validation/webview-native-smoke.json`，本机运行通过。
- `MacOsGpuWindow` 补齐可调用的 content resize、最小化、恢复、最小化状态查询与有界 AppKit 事件泵。真实 AppKit lifecycle smoke 创建窗口后完成 640×360→800×450 resize、最小化和恢复，并从正式 delegate 输入通道收回 1 个 resize、1 个 hidden、1 个 visible 事件。该 smoke 已接入 `TEST-COMPOSE-001`，输出由独立脚本保存为验收附件。
- Studio 原生构建复查同时修复 Tauri 事件 payload 缺少 `Clone` 的编译阻断。Studio Svelte/TypeScript 检查为 0 error、0 warning，独立 target 的 Tauri Rust check 通过。
- 统一 runner 已执行完整 `TEST-COMPOSE-001`：Voplay–Vogui 组合单元测试、真实 Metal 三帧合成与 accessibility action、真实 AppKit resize/minimize/restore、真实双代 Tauri WebView bridge/recovery 四段全部通过。AppKit 与 WebView 附件 SHA-256 分别为 `00362e3eafd6b8cfd9c991cbe1908ecd75cd9021962757367ddcc0f851173790`、`7ee5c47b060c9f1b517b396c3252c5420e031bef1a58c56bf2eb8e32af30d732`；候选报告继续因三仓 dirty identity 保持不可认证。

## 2026-07-26 统一交付门复验

- 三套前端静态检查完成：Vogui JS 与 Voplay Web TypeScript 通过，Studio Svelte/TypeScript 为 0 error/0 warning；同时通过 Studio `test:site`、guest-exit 协议与 Vite 生产构建。复验期间修复 Voplay SurfaceHost 异步初始化后的空值收窄，以及 Studio platform-event 联合类型与快捷键签名边界。
- Volang 使用独立 target 完成 `./d.py test`，结果为 2357 passed、0 failed。Vogui 使用独立 clean target 完成 workspace all-targets 测试，runtime 66 tests 及 protocol/layout/text/native/headless/codegen/benchmark 均通过。Voplay 在最终修复后重新完成 workspace all-targets 测试，runtime 197 tests、Voplay–Vogui 2 tests、editor 4 tests、stable-scene benchmark 及其余 crate 全部通过。
- profile gate 捕获并修复 Voplay instrumentation 与 extension 的条件编译缺口：core profile 曾无条件引用 render/audio endpoint、platform backend 与 render bootstrap/result sink。修复后 Voplay core、2D、3D、editor profile 均通过精确 no-default-features 编译；Vogui headless、wasm32 web-minimal、native-webview-minimal 与完整 native-gpu 闭包均通过。
- App/Vogui/Voplay 三套协议生成 `--check` 通过；App protocol provenance 已由受治理生成器刷新。`vo-dev` traceability、repo-boundaries、layout、docs、skill、studio-web、examples、benchmarks、release lint 与 release matrix 通过，三仓 `git diff --check` 通过。
- 本机真实宿主再次通过：CoreAudio smoke 输出 `active_callbacks=1, recovered_callbacks=5, settled_callbacks=5, callback_errors=0`；macOS AppKit/Metal/WGPU/IME/accessibility 组合 smoke 输出 `presented_frames=3, accessibility_revision=1`。
- `lint artifacts` 仍因 App protocol 生成物未进入 Git 索引而失败；`lint studio-tauri` 仍缺少指向当前 Vogui 重写 commit 的精确 `vogui-protocol` Git revision。当前源码没有稳定 commit identity，因此仍不创建 EVID，不推进 ACC 到 verified/accepted，不修改 Quick Play 固定 revision 归档。

## 2026-07-26 输入链路验收补强

- `TEST-INPUT-001` 新增真实 Chromium 输入驱动。它直接打包 Voplay 的 `framework_lane` 与 `BrowserGamepadInputSource` 源码，在隔离浏览器中验证 pointer、key、text、IME composition、focus 与 gamepad 的 typed `PlatformInput` packet、Surface route、设备命名空间和 generation，并通过真实 RAF polling 验证 Gamepad connect、button、axis、热替换、禁用释放、disconnect 与 owner close。
- 浏览器驱动发现 Voplay 合成 Gamepad button/axis 归零事件缺少 `synthesized` 来源标记；`BrowserGamepadInputSource` 已在 replacement、focus/visibility invalidation、禁用和 close 的 release 路径统一标记合成事件。修复后 Chromium smoke 两组场景全部通过。
- App Runtime 原验收命令曾因缺少 `platform_input` 单元测试而得到 0-test 空通过。当前新增两项正式路由用例：一项验证 focus loss 同时合成 pointer cancel、key up 与 gamepad disconnect 并清空 binding；另一项验证 capacity rejection 不消耗 device sequence，调用方可用相同 sequence 完成原 binding release。
- 统一 runner 已重新执行完整 `TEST-INPUT-001`：Voplay input 4 项、haptics 4 项、App Runtime platform input 2 项、Chromium 输入 smoke 和真实 AppKit resize/minimize/restore 全部通过。浏览器与 AppKit 附件 SHA-256 分别为 `94d19bb8d3e970753c71478128b4a01fc50682d45bee0f75b7112f8881f03c95`、`00362e3eafd6b8cfd9c991cbe1908ecd75cd9021962757367ddcc0f851173790`；候选报告因三仓 dirty identity 保持 `certifiable=false`。
- Voplay 平台 Chromium smoke 与 Vogui DOM renderer smoke 现直接输出 JSON 附件。前者通过 WebGPU 双 Session 三帧隔离呈现、Surface/Canvas2D 像素、haptics terminal、provider restart、asset lifecycle 和 audio user gesture 六组场景；后者通过 DOM renderer 场景。附件 SHA-256 分别为 `93b3e1cd46af0473e279a9bbe643073bf81e2a85dda3b8317112a1b08317c948`、`09dc4bfa009808a38050acc8dbcf13b43b05adb982e58d5604d517559e8d73f5`。
- `TEST-AUDIO-002` 已使用独立 wrapper 保存真实 CoreAudio 报告。Voplay audio 2 项 owner transition 测试通过；实际 output callback 在 device generation rebind 后继续运行，close 后 callback 数保持稳定且 callback error 为零。该次附件 SHA-256 为 `c8b617c0fac5e8fd82e1260f2f793838f21cbeba89856c052a3c8276ab541eda`。
- `TEST-PLATFORM-001` 已从单一 browser profile 扩为 `browser+native+device`，顺序执行 Chromium、AppKit 与 CoreAudio，并在一次候选运行中绑定三类直接附件。三段全部通过；该次 CoreAudio 报告为 `active_callbacks=3, recovered_callbacks=10, settled_callbacks=10, callback_errors=0`。
- `TEST-COMPOSE-001` 的真实 Metal/WGPU smoke 已改用独立 wrapper 保存附件。Voplay–Vogui 组合在 AppKit Window/CAMetalLayer 上 present 3 帧，同时完成 revision 1 accessibility action；GPU 附件 SHA-256 为 `b9c68150f94a50a64761870be59eee87a192cabc1d8824a6796cfacfbc67c22b`。随后 AppKit lifecycle 与双代 Tauri/WKWebView recovery 再次通过，完整组合候选报告通过。
- `TEST-RECOVERY-001` 现明确覆盖三仓 fault/soak。Voplay platform/runtime 共执行 device loss、surface incident、renderer fault、双 Engine recovery、pending remote shutdown 等 9 项；Vogui reload 3 项；Volang 与 Vogui 各执行 1 项原子 fault recovery。Volang Session owner graph、Voplay Runtime owner graph、Vogui UiSession owner graph各自完成 1,000 次 create/close 或 attach/detach，全部以 live resource 归零结束。
- `TEST-RECOVERY-002` 通过 native render resize/device-generation rebind、asset provider/work generation、audio permit generation 共 8 项单元测试，同时再次运行真实 Metal/WGPU 三帧与 CoreAudio generation rebind。该次 GPU 与 CoreAudio 附件 SHA-256 分别为 `b9c68150f94a50a64761870be59eee87a192cabc1d8824a6796cfacfbc67c22b`、`462ca9ebf4b3a174df8c361e22577952e95c98ee8888b2d4ec952b62f42fbb0d`。
- Voplay stable-scene 与 Vogui stable-UI benchmark 均改为保存 optimized JSON 附件。Voplay 在 10,000 object fixture 上执行 200 次单 object delta，`p50=1542ns`、`p95=1709ns`、`p99=1792ns`，并由断言证明每轮只 decode/queue 1 个对象；附件 SHA-256 为 `e26c84240652b5f42a6691b1f307236f132ecc58584c5ce2f2be375122a4e307`。
- Vogui 在 10,000 node fixture 上执行 200 次单 Scope reconcile，`p50=10125ns`、`p95=16000ns`、`p99=16292ns`，work-counter 断言 200 轮只新增 200 次 owned-node scan；附件 SHA-256 为 `417fd12d9a34e306bb9d2df3fc6e1946b7f55a8882edeea642f3275379d615a2`。
- `TEST-PROFILE-002` 已保存六个真实 wasm release artifact 的尺寸与依赖闭包 JSONL。Voplay core/2D/3D/full gzip 分别为 134260、323957、727358、865036 bytes，均低于各自 256000、921600、1572864、1887436 限额；每项均记录 artifact 与 dependency tree SHA-256。Vogui web-minimal/web-full gzip 分别为 339276、339796 bytes，同样记录完整 digest。
- Voplay core/2D/3D/editor 和 Vogui headless/web-minimal/native-webview-minimal 的精确 no-default-features 编译矩阵全部通过。Voplay 与 Vogui profile JSONL 附件 SHA-256 分别为 `9e47c829e1ec4741b86e61aea432da8edd485896819937912352610494da9ad3`、`0c7ff0431f2a9120d555b7c665dfd795f2d4e08fb02a396b0cc4aa06b6231468`。
- App protocol 与跨 Vogui/Voplay framework decoder 的 100,000 次固定 seed sancov fuzz 现保存 summary JSON 和完整 libFuzzer campaign log。App campaign 最终 `ft=218, corp=61/3732b`；framework campaign 最终 `ft=1162, corp=331/16Kb`，两者均完成全部 runs 且无 crash/panic。App summary/log SHA-256 分别为 `5fcfdc52edec25f397a922c75c3890cfa6156a1b2f7b60c5a35f14f252c958df`、`f6af68b1d7f67c23f305bd27862cb297eef37fed3bcf27baedac014d1c3ecf26`；framework summary/log 分别为 `547a916f042354cd72fe981fa24ca4724261d3d96a13c191c63b74bcd0da44b5`、`0d172a05b052b8469a1d7358993d5bea2d881e947ec7d58c761f7dd41d477dff`。
- 普通消费者 smoke 现输出纯 JSON summary 与完整 `work sync/check` log。它在隔离 module/generator cache 中构建 Volang CLI 和两套 generator provider，分别以 Vogui headless 与 Voplay core fixture 生成 lock、同步 workspace 并完成静态检查，同时确认 governed generated outputs 没有写回消费者源码树。summary/log SHA-256 分别为 `c8d6d53d80f2241ee694c9ffd2f47c325c81349e7b6c2e3d0eaf7ddc84c5b7f2`、`f5d7934f9fd2f86551a1626052d796ec7364f1d789c13f839682191a2988fe1f`。

## 2026-07-26 依赖顺序候选验收收口

- `TEST-RT-001` 的共享 Cargo 增量目录出现过旧依赖元数据失配；同命令在全新临时 target 中完整通过，验收清单随后为 `vo-web` 固定独立 target，避免与其他 profile 构建相互污染。重跑结果为 App Runtime 107 项、Runtime HostServices 8 项、VM runtime boundary 87 项、Web 81 项全部通过，Studio 静态检查为 0 error/0 warning。
- Voplay 依赖链按 Engine → World → Render → Asset → Audio → Render Extension → Multiview 顺序执行。`TEST-ENGINE-001`、`TEST-WORLD-001`、`TEST-RENDER-001/002`、`TEST-ASSET-001/002`、`TEST-AUDIO-001`、`TEST-RENDEREXT-001`、`TEST-MULTIVIEW-001` 均生成 passing 候选报告；WASM 3D RenderFeature 的 `wasm32-unknown-unknown` 精确闭包同时通过。
- Vogui 依赖链按 App transaction → Event → Resource → Widget → Layout/Text/Renderer/Accessibility 顺序执行。`TEST-GUI-APP-001`、`TEST-GUI-EVENT-001`、`TEST-GUI-RESOURCE-001`、`TEST-GUI-WIDGET-001`、`TEST-GUI-RENDER-001` 均生成 passing 候选报告。
- 验收复查发现 `vo-module profile` 与旧 `transaction::tests` 过滤器各自命中 0 项。Volang ProfileCatalog/CapabilityCatalog 新增 3 项直接测试，覆盖继承/default/direct capability 归一化、cycle/unknown default/empty profile 拒绝，以及 requires/conflicts/target/import 合同；Vogui App 事务命令改为明确命中 multi-root prepare 原子性和 commit revision 两项测试。修正后的 `TEST-PROFILE-001` 与 `TEST-GUI-APP-001` 已重跑通过。
- 32 个 TEST ID 均已有机器生成的候选 JSON 报告；其中 `TEST-CI-001` 如实失败于稳定身份门，其余报告通过。全部报告继续记录 `certifiable=false`，原因是三仓工作树尚未提交。最终阶段仍需依次建立 Vogui、Voplay、Volang 稳定 commit，更新 Studio 的 Vogui Git pin、将受治理生成物纳入对应 commit、重建固定 revision 制品，再对同一身份执行最终报告并创建 EVID/推进 ACC。

## 2026-07-26 最终认证

- 已按依赖顺序建立稳定本地身份：Vogui `a5bf43f92f02667dcd339dee8975eb45e1ccf21e`、Voplay `ca4df3e812544a506770c9acd7bac213eaef8afa`、Volang 候选 `d3533818ad0477cc34544eb00bcc691901098488`。Studio 与 `eng/project.toml` 使用精确 revision。
- 最终候选上完整重跑 `eng/rewrite-validation.toml` 声明的 32 个 TEST，32/32 均为 `passed=true`、`certifiable=true`，所有报告绑定同一组三仓 clean source identity。
- `vo-dev rewrite certify` 完成报告、命令、附件、SHA-256、平台、工具链、直接证据关系与三仓身份核验，生成 89 条 EVID。
- `rewrite-traceability.toml` 已收敛为 24/24 REQ accepted、32/32 TEST accepted、86/86 ACC accepted；每项 ACC 的每个 TEST 均具有直接 passing EVID。
- 本次授权仅用于本地 commit。未执行 tag、push、PR、发布或部署。

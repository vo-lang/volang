// Regression: a struct containing only comparable fields is a valid map key.
package main

import (
	"fmt"
)

type Key struct {
	Name string
}

func main() {
	m := map[Key]int{}
	m[Key{"alice"}] = 100
	assert(m[Key{"alice"}] == 100, "lookup failed")
	fmt.Println("bug_map_struct_key_string: ok")
}

package main

import (
	"context"
	"time"
)

func main() {
	ctx, cancel := context.WithTimeout(context.Background(), 30 * time.Second)
	cancel()
	<-ctx.Done()
	assert(ctx.Err() == context.Canceled)
}

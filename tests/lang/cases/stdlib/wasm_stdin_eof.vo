package main

import (
	"fmt"
)

func main() {
	values, err := fmt.Scan()
	assert(values == nil, "EOF scan must not fabricate values")
	assert(err != nil && err.Error() == "EOF", "empty standard input must report EOF")
	println("wasm-stdin-eof: PASS")
}

package main

func main() {
	var badVal int = 10  // Not any, just int
	
	// Two named returns + capture int (not any)
	fn := func() (a int, b bool) {
		_ = badVal
		a = 1
		b = true
		return
	}
	r, o := fn()
	println(r, o)
}

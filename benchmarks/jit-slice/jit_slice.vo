package main

func main() {
	n := 500000
	xs := make([]int, 0, n)
	sum := 0

	for i := 0; i < n; i++ {
		xs = append(xs, i)
	}
	for i := 0; i < len(xs); i++ {
		xs[i] = xs[i] + 1
	}
	mid := xs[17 : len(xs)-17]
	for i := 0; i < len(mid); i += 3 {
		sum += mid[i]
	}

	println(sum)
}

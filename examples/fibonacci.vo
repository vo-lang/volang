package main

func fib(n int) int {
    if n <= 1 {
        return n
    }
    return fib(n-1) + fib(n-2)
}

func main() int {
    for i := 0; i < 20; i++ {
        println(fib(i))
    }
    return 0
}

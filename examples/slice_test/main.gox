package main

func main() {
    var s []int
    s = append(s, 10)
    s = append(s, 20)
    println(len(s))
    
    // Simple for-range
    for i, v := range s {
        println(i)
        println(v)
    }
    println(999)
}
